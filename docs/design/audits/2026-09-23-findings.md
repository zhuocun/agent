# Audit run 2026-09-23 — raw findings

Triage input for ISSUES.md from the `pnpm audit:ui` run (453 captures). Each F-item is now recorded as an ISSUE in `docs/design/audits/ISSUES.md`, section "2026-09-23 audit run". Fn maps to ISSUE-(n+9):

F1 → ISSUE-10 (fix in review), F2 → ISSUE-11, F3 → ISSUE-12, F4 → ISSUE-13, F5 → ISSUE-14, F6 → ISSUE-15, F7 → ISSUE-16, F8 → ISSUE-17, F9 → ISSUE-18, F10 → ISSUE-19, F11 → ISSUE-20, F12 → ISSUE-21, F13 → ISSUE-22, F14 → ISSUE-23, F15 → ISSUE-24, F16 → ISSUE-25, F17 → ISSUE-26, F18 → ISSUE-27.

Later findings from verifying the fixes are not listed below. They are ISSUE-28–33: settings disclosure touch floor, temporary-chat menu covering Turn off, message overflow menu focus, Models/Shortcuts scrollers, the spend caption's contrast, and citation hit-area overlap (§15 C50). The "Known (not counted)" line is recorded under "known, not counted" in ISSUES.md.

MAJOR
F1 Stop then any new send -> 409 STREAM_IN_PROGRESS (10/10, persists >=12s); user bubble removed, draft lost; zero turn.stopped logs; stopped partial never reaches share view. Hypothesis: sse-starlette cancellation also cancels stop-path DB write (api/app/streaming/handler.py, turn_lifecycle.py _terminalize). FE half: chat-thread.tsx:1183.
F2 Below 768px, mouse/pen clicks inside bottom sheets are dead (keyboard works; p600 capture). use-swipe-dismiss.ts:185 setPointerCapture on pointerdown.
F3 Agentic "Partial answer — some research steps did not finish" shows before the plan is approved: server sends partial=True at pause (orchestrator.py:520); agentic-layout.ts:99 lacks pause guard. NIT: plan card stale "Running" after run done.
MINOR
F4 Table cells break mid-word, table never scrolls (globals.css:842 overflow-wrap:anywhere).
F5 320x640 welcome overflows upward: "Connect your API key" under header, title collides with right pill (welcome-screen.tsx:89).
F6 Auth dialog clips left edge of input focus ring (auth-dialog.tsx:168).
F7 Command palette ARIA invalid, axe critical (command-palette.tsx:837-856).
F8 text-muted-foreground/70 at 12-13px 3.06-4.04:1 (subagent-panel, tool-part, tier-picker).
F9 Citation markers [1] 22x13 px < 24px pointer (markdown-renderer.tsx:116).
F10 820 tablet touch targets <44: sidebar Advanced search 24h, Select 36, "Thought for" 28, sources toggle 28.
F11 Composer breaks at 200% text size.
F12 Code-block and table scrollers not keyboard-reachable (scrollable-region-focusable).
F13 Toast list markup invalid.
F14 Model picker nests a switch inside a button.
NIT
F15 Double divider under Platform credits.
F16 Missing space "$0.0001/ $1.00".
F17 Mobile drawer bottom strip mismatched color.
F18 Model picker section headings repeat as the only row label.
Known (not counted): Fast label wrap, C40, C48, C15.
