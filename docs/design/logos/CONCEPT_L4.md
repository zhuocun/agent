# Concept L4 — "Lattice" (system / synthesis)

> **Lane L4** per revised constraint sheet. **Interwoven cross-hatch** — offset
> skewed grid reading as composable system structure. Explored but not selected.

---

## Concept

Two vertical + two horizontal heavy monoline strokes forming an offset,
italic-skewed cross-hatch (`#` leaning ~12°), with flat value steps between
front (`brand`) and back (`deep`) pairs. Central square counter.

---

## Status

**Rejected at down-select.** Reads as literal hashtag; overlapping depth steps
brush charter flatness rules. Lead concept is **L3 Fork**.

---

## IMAGE_GENERATION_PROMPT — Symbol dark (hero)

```
BOLD flat vector logo on square canvas #080B11. A heavy interwoven cross-hatch glyph — two vertical and two horizontal very thick monoline strokes, slightly italic-skewed for forward motion, round caps, flat luminous blue #5BA0F2 with the back pair a flat deeper blue #14539C for layered depth, hard crisp edges between the two flat blue values, enclosing a small central square counter, filling 80% of frame height. NO circles NO rings NO letter O NO stacked rectangles NO slot NO text. Woven system lattice symbol. NO gradient NO blend NO fade NO glow NO shadow NO 3D NO bevel NO second hue.
```

Assets: `assets/concept-l4/olune-logo-l4-lattice-symbol-{light,dark}.png`
