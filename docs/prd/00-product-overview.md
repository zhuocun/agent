# PRD 00 — Product Overview & Vision

**Product (working):** A transparent, multi-model, cost-leading AI chat for web and mobile (mobile-web first).
**Status:** Draft for build. This is the integrating document for the PRD set.
**Date:** 2026-05-27.
**Owner:** Product.

> **What this document is.** The single entry point to the PRD set. It states the vision, the problem, the target users, the positioning, the MVP scope (and what is explicitly deferred), and how the five workstream PRDs fit together. Domain detail lives in PRDs 01–05; this document does not duplicate it — it frames and links it.

---

## 1. Vision & positioning

**"Every major model in one place — where you see (and control) the cost and your data."**

We are building a consumer/prosumer AI chat product comparable to the web and mobile-web experiences of ChatGPT, Claude, Gemini, and Perplexity — but differentiated on a wedge none of them own cleanly: **multi-model choice + transparency + aggressive cost leadership** (DeepSeek as the main default provider), with **privacy as a configurable posture** (no-train-by-default, BYOK) rather than a hard provider gate.

The market gap (see PRD 05 §1, §3):
- Incumbents are **single-model** (ChatGPT/Claude/Gemini) — you can't pick the best model per task in one place.
- Perplexity is **trust-damaged** on transparency (reported silent model downgrades).
- Aggregators (Poe) offer many models but **no privacy/transparency story**.
- Most incumbents **train on chats by default** or quietly extended retention, and **under-invest in mobile-web**.

We stack defensible wedges — **model choice, transparency (which model answered + what it cost), and aggressive cost leadership (DeepSeek default + BYOK)**, with privacy as a configurable posture — and win the core experience on **streaming/rendering fidelity, composer ergonomics, accessibility, and mobile-web polish** rather than feature breadth.

**What we are not:** a generic AI work assistant, inbox/calendar agent, or "do everything for your job" productivity suite. We are a **chat product** whose wedge is **multi-model choice + per-message cost/model transparency + privacy/BYOK** — winning on the core chat loop and trust surfaces, not feature breadth.

---

## 2. Problem & opportunity

Power users and developers increasingly use multiple AI models but are forced to juggle several single-model subscriptions, can't see which model produced an answer or what it cost, and have weak guarantees about data training/retention. There is room for a **model-agnostic, transparent, privacy-first** product that is also genuinely excellent on mobile-web — an area incumbents treat as an afterthought.

---

## 3. Goals & non-goals (product level)

### Goals
1. Establish a **defensible beachhead with power users / developers** on the multi-model + transparency + privacy wedge.
2. Ship a **credible MVP fast** that feels faster and more polished than incumbents on the fundamentals.
3. **Protect gross margin from day one** (aggressive model routing, metered free tier, BYOK) given AI-economics reality.
4. Make **trust a product surface**: always show the model used and the cost; never silently downgrade.
5. Stay **expandable**: keep a simple default mode so we can move down-market (casual) and up-market (teams) later without a rebuild.

### Non-goals (this phase)
- Competing on raw feature breadth at the commoditized ~$20 single-model tier.
- Casual mass-market acquisition, enterprise seats / SSO / SOC 2, and ads — all deferred (see PRD 05 §2.2, §4).
- Native mobile apps at launch (responsive web + PWA first; Capacitor later — PRD 03 §6).

---

## 4. Target users

| Priority | Persona | Why them |
|---|---|---|
| **Primary (MVP)** | **Power users / developers** | They value model choice, transparency, and cost control — our exact wedges — will pay or BYOK (protecting margin), and are cheap to reach. |
| **Secondary (fast-follow)** | **Privacy-conscious prosumers** (researchers, journalists, lawyers, EU users) | Drawn by no-train-by-default + transparency + GDPR posture; low incremental build cost. |
| **Deferred (P2+)** | Teams / orgs, then enterprise; casual mass-market | High value or high reach but long cycle / high COGS — expand once the beachhead is solid. |

Canonical persona/positioning detail: **PRD 05 §3.**

---

## 5. MVP scope (the headline decision)

The MVP is a **lean, text-core, multi-model chat** that is polished, accessible, and mobile-web-first. Two scope decisions anchor the set:

- **Lean text-core MVP.** P0 is a polished multi-model **text** chat + the transparency surface + the core UX. **Vision/PDF input, tool/function-calling, and live web-search/citations are deferred to P1** (designed-for now, built next).
- **BYOK ships at launch (P0).** Bring-your-own-key is the power-user margin de-risk and a privacy/cost-control wedge; the architecture designs encrypted key handling from day one.

### In the MVP (P0)
Streaming chat with stop-that-preserves-partial-output · interrupted-stream recovery (**partial + Continue/Regenerate**, not P1 replay) · streaming-safe markdown rendering (code+copy, KaTeX, GFM tables; **Mermaid diagrams → P1**) · collapsible reasoning panel · robust **text-only** composer + model-tier picker · **multi-provider model picker + per-message transparency (model used + token/cost)** · **transparency cost-accounting schema rich enough for tiered/threshold/cached/promo pricing + reasoning tokens, with a served-vs-requested model surface** · **typed multi-part message model** (data layer is full; renderer ships the text/code/reasoning/status subset) · heuristic auto-routing to a cheap **DeepSeek** default model (the cost-leading main provider) · xAI/Grok in the registry but excluded from Auto/default until data-policy review · conversation management (history, rename, delete, search) · core message actions (copy, regenerate, edit-last, thumbs) · system prompt + custom instructions · context management + usage/cost meter · structured outputs + schema validation · baseline safety/moderation · onboarding empty state · settings (theme, custom instructions, data controls; BYOK only for non-anonymous accounts) · share link + copy-as-markdown · keyboard shortcuts + `Cmd/Ctrl+K` palette · **accessibility baseline** · **P0 i18n baseline** (externalized strings, RTL/logical CSS, IME-safe composer) · **responsive web + PWA** · optimistic send + offline draft/queue + virtualized message list · **INP performance budget** (`scheduler.yield()` + rAF token-batching) · **no-train-by-default + retention controls + export/delete + AI-interaction disclosure** · **metered free tier + Pro (metered caps + transparent USD credit overage; price open — see §12) + BYOK** · **minimal metered-overage/credit primitive** · public share link + GDPR export/delete · first-party analytics · per-turn reasoning-effort override · provider fallback · per-user monthly budget cap.

### Deferred
- **P1 (fast-follow) — several have shipped ahead of schedule (see §11 D15/D18), with truthful gating:** vision/PDF + attachments UI (*partially shipped — attach + text/PDF transcript on attachment-capable bindings (prod DeepSeek accepts none); image vision Anthropic-only*), Mermaid (*shipped*), tool/function-calling + agentic loop with HITL approval (*shipped\* — `TOOLS_ENABLED`, fake-provider v1*), web-search + live citations (*shipped\* — `SEARCH_BACKEND` default `none`, Tavily-ready; inline `[n]` citation markers now shipped over the source-card list — D24, #143*), inline `[n]` citations + citation transparency contract (*shipped — D24, #143; clickable/hover-to-source markers bound to `SourceItem.id`, grounded-vs-ungrounded honesty rule, provenance label, share/export asymmetry, backend `requested`/provenance — gated behind web search (`SEARCH_BACKEND`, default `none`) like the search it annotates*), copy-on-branch (*shipped*), native slash commands (*shipped — client-static prefill*), resumable-stream replay (*shipped\* — `RESUMABLE_STREAMS_ENABLED`*), longitudinal spend analytics + value-aware picker + proactive budget guardrails (*shipped — D27, #144; `GET /api/account/spend` (daily/by-model/by-conversation, cumulative vs surviving-messages bases), per-conversation budget cap (migration `0019`) with 80%/100% soft-cap alerts, label-only "Cheapest" badge — no silent model switching*), data-access activity log + data-processing rollup + model/data-policy directory + transparent moderation + incident/status transparency (*shipped — D30, #145; `GET /api/account/activity`, `/api/account/data-processing`, `POST /api/account/moderation-appeal`, `GET /api/models/directory`, public `GET /api/status` + `/status` page + degraded banner — built on already-persisted data, no migration*). **Now shipped (feature-expansion pass — §11 D19–D32, roadmap in PRD 05 §4.5):** transparent long-term memory (D19, #148), Projects/Spaces (D20, #151), dictation + read-aloud / STT+TTS (D22, #149), prompt library + customizable keyboard shortcuts (D23, #151), granular per-conversation retention + scheduled purge (D31, #147), conversation org v2 — tags/archive/bulk actions (D20, #153), and advanced history search — transparency-native filters + query-time Postgres FTS (D21, #153). **Still specced, NOT built:** ephemeral/incognito chat + sensitive tagging + multi-format (PDF/.docx) export (D31).
- **P2 (later) — now specced (feature-expansion pass; specced, NOT built):** artifacts/canvas + code execution + custom assistants/personas + MCP **action** connectors (D23), file/document RAG + retrieval privacy + retrieval-cost transparency (D25), read-only data-source connectors (D26), unified hands-free voice + in-thread image generation (D22; image-gen content-marking D32), referrals/gift credits + annual-plan lifecycle (D28), lightweight prosumer shared workspaces (D29), and **agentic mode — an in-turn orchestrator over bounded model subagents** (D33–D40; chat-anchored, default-OFF `AGENTIC_ENABLED`, fake-provider-first; PRD 02 §4.6 FR-26c–FR-26k, build plan `docs/plans/01-agentic-mode.md`). **Still deferred (no spec this pass):** trained routing, Capacitor native, enterprise teams/SSO/SOC2 (non-goal beyond the D29 prosumer slice), ads (revisit at scale), in-thread alternate-response trees.

> **Shipped beyond original scope:** a 2-up **parallel model comparison** view (same prompt → two tiers side-by-side) shipped (#134/#135) — originally slated P2 — plus a reasoning-effort picker, JSON-mode toggle, and regenerate-with-a-different-model menu.

> **Dropped (not a phase):** native **pull-to-refresh** — it reloads the page and kills in-flight streams; superseded by `overscroll-behavior: contain` (PRD 03 §4.4).

Full reconciled phase table with dependencies: **PRD 05 §4.**

---

## 6. The PRD set (how to read it)

| PRD | Scope |
|---|---|
| **00 — Overview** (this doc) | Vision, problem, personas, positioning, MVP scope, decisions log |
| **[01 — Core Chat Experience](01-core-chat-experience.md)** | The chat UI/UX: streaming, reasoning panel, composer, rendering, conversation mgmt, message actions, onboarding, settings, sharing, shortcuts, a11y |
| **[02 — AI Capabilities & Model Layer](02-ai-capabilities.md)** | Provider abstraction + data-driven model registry, picker/routing, reasoning, context mgmt, structured outputs, safety; tools/search/vision (P1) |
| **[03 — Mobile & Cross-Platform](03-mobile-cross-platform.md)** | Responsive pane model, mobile composer/keyboard, gestures, offline, PWA, performance, delivery (PWA→Capacitor) |
| **[04 — Technical Architecture](04-technical-architecture.md)** | Stack, streaming/resumable, data model, provider/BYOK, auth/guest, storage, security/privacy NFRs, deployment |
| **[05 — Roadmap, Monetization, Metrics & NFRs](05-roadmap-monetization-metrics.md)** | Reconciled P0/P1/P2 roadmap, monetization, KPIs, accessibility/i18n/privacy/compliance NFRs, risks |
| **[06 — Design System & Visual Spec](06-design-system-visual-spec.md)** | Tokens, layout primitives, chat components, transparency/privacy chrome, visual acceptance criteria |
| **[07 — Transparency Contract](07-transparency-contract.md)** | End-to-end model + cost + served-vs-requested contract across registry, persistence, UI, metering, and share/export rules |
| **[08 — Error & Limit States](08-error-and-limit-states.md)** | Unified taxonomy and UX for stream failures, provider/app errors, quota limits, guest gates, BYOK failures, and offline states |

> **Numbering note:** PRD numbers ≠ research-file numbers (e.g. research-04 *AI capabilities* → PRD 02; research-02 *mobile* → PRD 03; research-03 *architecture* → PRD 04).

---

## 7. Cross-cutting principles

- **Transparency as product:** every assistant message shows which model answered and (where available) its token/cost. Never silently downgrade. This is a single **transparency contract** — a cost-accounting schema rich enough to be *true* (tiered/threshold/cached/promo pricing + reasoning tokens) plus a **served-vs-requested** model surface — cutting across PRD 01 (UX/display), PRD 02 (registry + pricing schema), PRD 04 (data-model capture), and PRD 07 (contract authority). It is a **P0 cross-cutting workstream with one named owner**, not three loosely-coupled pieces. **Public share exception:** unlisted read-only shares show model attribution but omit token/cost fields; in-app threads and private export retain full transparency.
- **Privacy-first:** no training on user chats by default; short configurable retention; one-click export/delete; optional no-telemetry mode.
- **Accessibility is a differentiation lever, not a follow-up:** labeled controls, ARIA live regions for streaming, full keyboard operability — incumbents have measured gaps (PRD 01 §5.7, PRD 05 §7.1).
- **Mobile-web first:** the product is excellent on a phone browser, not a shrunk desktop UI (PRD 03).
- **Multi-provider from day one** behind a thin abstraction + data-driven model registry; **never hardcode** model IDs/prices/context windows (PRD 02 §5).
- **MVP-fast-but-not-cornered:** pragmatic hosted defaults, every external dependency behind a thin adapter so a later move is a migration, not a rewrite (PRD 04). Current implementation uses Vercel for the FE and Fly for the FastAPI backend.

---

## 8. Technical foundation (summary)

**Current implementation:** **Next.js 16** App Router FE on Vercel (responsive web + PWA) calling same-origin `/api/*`, with Next rewriting to a **FastAPI** backend on Fly.io. The backend uses SQLAlchemy 2.0 async + Alembic on Neon Postgres, custom signed-cookie anonymous sessions with in-place upgrade/login, SSE via `sse-starlette`, DeepSeek through an OpenAI-compatible adapter as the canonical provider, optional Anthropic/OpenAI/fake backends, AES-GCM encrypted BYOK keys, slowapi rate limits, best-effort usage-budget enforcement, and optional Sentry/OpenTelemetry. The data model uses typed multi-part message parts and captures served/requested model attribution plus per-message cost/usage for the transparency contract. Full detail and implementation notes: **PRD 04** plus `AGENTS.md` / `api/README.md` as ops truth.

---

## 9. Success metrics (summary)

Instrument from day one: activation / time-to-first-value · a first-week **"Day-1 success" activation funnel** (% completing a first-week success checklist — the strongest countermeasure to AI-tourist churn) · **time-to-first-token + per-model latency** · **AI-native retention (≈40% GRR / 48% NRR — model on AI-native, not SaaS-optimistic, benchmarks)** + D1/D7/D30 **plus task-recurrence** (AI usage is bursty) · DAU/MAU stickiness · messages/session · free→paid conversion / MRR / churn · **ARPU target bands (sub-led ~$30–100+; hybrid ~$3–15)** · **cost-per-message / gross margin / model-routing mix** · stream terminal events (`completed` / `stopped` / `error` / `interrupted`) · thumbs/regeneration/NPS. Definitions, benchmarks, and phase-2 metrics: **PRD 05 §6.**

---

## 10. Top risks (summary)

Commoditization vs incumbents · **aggregator price-compression** (T3 Chat @ $8/mo multi-model undercuts the same surface — the sharper near-term risk than generic commoditization) · **pricing-model risk** (shipping a flat sub while the 2026 market reprices to metered credits) · COGS/margin erosion ("inference whales", 30–100× token variance) · trust/privacy execution · smaller power-user TAM · upstream provider dependency · **EU AI Act compliance timeline** — AI-interaction disclosure is **firm at ~Aug 2 2026**, but **content-marking (Art. 50(2)) remains legally unsettled** (readings range Aug 2–Dec 2 2026; the May-2026 Digital Omnibus is provisional pending Official Journal) and **needs legal sign-off before EU-launch scope is locked** · **US state-law surface (beyond EU):** CA SB 243 + AB 2013 (live 1/1/26); **CO SB 205 is no longer a live gate** (enforcement stayed Apr 2026; repealed/replaced by SB 26-189 → effective Jan 1 2027, narrowed to ADMT) — the US disclosure gate now rests on CA SB 243 + good practice. Mitigations: **PRD 05 §8.**

---

## 11. Decisions log

| # | Decision | Rationale |
|---|---|---|
| D1 | **Positioning = transparent, multi-model, cost-leading chat for power users.** Privacy is a configurable posture (no-train-by-default, retention controls, BYOK), not a hard provider gate. | Open market gap; stacks defensible wedges — model choice + per-message transparency + aggressive cost leadership (DeepSeek as the main default provider) — vs single-model / trust-damaged incumbents. Privacy remains a product control, but it no longer excludes the cheapest capable route. |
| D2 | **Multi-provider model picker + per-message transparency is IN the MVP** (not a later phase). | It is the core wedge and is cheap via the provider-abstraction layer + gateway. (Resolved a research-01 vs research-05 conflict in favor of 05.) |
| D3 | **Lean text-core MVP:** vision/PDF, tool-calling, and web-search are **P1**, not P0. | Focus the MVP on a polished, accessible, mobile-web text chat; defer heavier multimodal/agentic/retrieval infra. (Resolved a PRD-02 vs PRD-05 P0/P1 conflict.) |
| D4 | **BYOK ships at launch (P0).** | Power-user margin de-risk + privacy/cost-control wedge; architecture designs encrypted key handling from day one. (Aligned PRDs 02/04/05.) |
| D5 | **Delivery = responsive web + PWA now; Capacitor native later** (trigger-based). React Native rejected (full UI rewrite). | Single codebase, fastest to market, ~100% reuse into Capacitor when iOS push / app-store / durable-offline triggers fire. |
| D6 | **Transparency contract = a single P0 cross-cutting workstream with one named owner**, spanning PRD 01 (UX/display), PRD 02 (registry + cost-accounting schema), PRD 04 (data-model capture). The cost-accounting schema must be rich enough to be *true* — **tiered/threshold/cached/promo pricing + reasoning-token cost** — plus a **served-vs-requested model** surface with reason-for-substitution. | It is the core wedge and was left generic across three PRDs, so the promise wasn't end-to-end true; a scalar price is *wrong on exactly the high-value long-context turns*. One owner, schema as the spine. |
| D7 | **Typed multi-part message model in the P0 data layer** (ordered `text \| reasoning \| tool-call \| tool-result \| citation \| interactive-block` parts); P0 renders only the text/code/reasoning subset. | Cheapest high-leverage decision in the set — de-risks tools/citations/interactive-viz/generative-UI (all P1/P2) in one move; modeling a message as one markdown string guarantees a P1 refactor. Made once in the data model (PRD 04), referenced by PRD 01. |
| D8 | **Minimal metered-overage / transparent USD credit primitive in P0** (was P1): Pro = explicit metered caps + transparent overage, not a flat "generous limits" plan. | The 2026 market reprices to usage/credits (Copilot 6/1/26, Anthropic, Cursor); a flat sub re-creates the "inference whale" risk the PRDs warn about. The P0 cost meter is already the spine; enforcement owned by PRD 04, monetization decision by PRD 05. Resolves the PRD 05 §5.1-vs-§5.2 flat-vs-metered inconsistency. |
| D9 | **Foundation shipped: Next.js 16 FE + FastAPI/Fly backend + SQLAlchemy/Alembic on Neon + custom signed-cookie anonymous sessions.** The earlier AI SDK v6 / Better Auth / Drizzle / Upstash architecture is superseded as implementation truth. | Matches the deployed repo and ops model while preserving the adapter boundaries needed for future provider/gateway/auth migrations (PRD 04 §4/§5.5/§9). |
| D10 | **Explicit copy-on-branch ("branch in new chat") → P1** (was P2); in-thread alternate-response trees stay P2+. | Corrects a factual error (incumbents *shipped* explicit branching, didn't retreat); low-risk copy model that directly serves the dev/power-user beachhead (PRD 01 §4.6/§8). |
| D11 | **DeepSeek is the main provider and the default route across the tier ladder** (`deepseek-v4-flash` for fast/smart/auto, `deepseek-v4-pro` for pro in the current registry), via the OpenAI-compatible binding; PRD 02 owns the registry. Anthropic ships as an alternate backend; other Western routes remain selectable in the picker. | DeepSeek is the cost leader (token prices 30–100× below frontier) and exposes thinking tokens on supported routes — it carries the metered free tier and the Auto default. The data-residency posture (data processed in mainland China; jurisdictional bans) is a **known, accepted tradeoff** surfaced via the `data_policy` badge, not a blocker; privacy-sensitive users can pick a Western route or use BYOK. |
| D12 | **Mermaid interactive rendering → P1; P0 ships code/math/tables only.** | Resolves cross-PRD scope drift and avoids P0 bundle bloat. Fenced Mermaid may render as code until P1. |
| D13 | **Mobile attach/camera UI → P1 with vision/PDF; P0 mobile composer is text-only.** | Resolves mobile PRD attach affordance drift against the lean text-core MVP. |
| D14 | **P0 interrupted-stream recovery ≠ P1 resumable replay.** | P0 preserves partial output and offers Continue/Regenerate; P1 replays buffered deltas from the same stream id. Keeping these separate avoids billing, persistence, and UX bugs. |
| D15 | **Tools/HITL, web search, resumable replay, safety preflight, and billing shipped ahead of/within phase but gated default-OFF** (`TOOLS_ENABLED`, `SEARCH_BACKEND`, `RESUMABLE_STREAMS_ENABLED`, `SAFETY_BACKEND=local`, `BILLING_BACKEND`). | Land engine + schema behind hard safety flags so prod is byte-identical until deliberately enabled; de-risks the highest-risk paths (agent loop, detached streaming, payments). Tool v1 drives the fake provider only; replay is prod-gated on Redis. |
| D16 | **Real Stripe billing + USD credit ledger shipped in P0** (default-off `BILLING_BACKEND`): Checkout (Pro + credit packs), Portal, signature-verified idempotent webhooks, Pro entitlements, grant/debit ledger read by the budget gate. | The P0 metered-overage primitive (D8) needed a real settlement path; ships the spine while inert until Stripe is configured. |
| D17 | **Privacy/platform surfaces shipped in P0:** public cost-stripped share links, GDPR export + right-to-erasure, first-party analytics, custom instructions, per-user retention + monthly budget cap. | Scattered across P1/no-FE-surface in the workstream PRDs but core to the transparency/privacy wedge and built. |
| D18 | **Vision is Anthropic-only at launch; per-turn reasoning-effort override and provider fallback are shipped.** | The default DeepSeek/OpenAI-compatible binding is `supports_vision=False`, so image interpretation requires the Anthropic backend (attachments + text/PDF-as-transcript work on attachment-capable bindings; the prod DeepSeek route accepts none). Reasoning-effort override and pre-first-token substitution-coded provider fallback are live. |

> **D19–D32 — Feature-expansion pass (this brainstorm).** The decisions below cover a set of **net-new, on-strategy features specced (not yet built)** to make the product more complete. They were authored against the shipped implementation and the existing PRD set, each deepening one of the six wedges (multi-model · transparency · cost · privacy · mobile-web · accessibility) rather than chasing breadth. Full feature specs live in the workstream PRDs (01/02/03/04/06/07/08); the roadmap placement is **PRD 05 §4.5**; this log records the strategic calls + guardrails. **D24 (#143), D27 (#144), and D30 (#145) have since shipped on `main`** (see their rows below and the §5 shipped list); the remaining D19–D32 decisions are specs for build.

| D19 | **Long-term memory ships as an opt-in, fully user-visible editable store with per-fact provenance** (the glass-box "transparency layer", not generic memory): off by default, extracted off the request path, injected into the cache-stable prefix (PRD 02 FR-37), excluded from temporary chats, surfaced per-answer via a "memory used here" indicator, and complete in export/erase. | Memory is a commoditized baseline at incumbents but opaque; our wedge is the editable, attributed ledger + per-message indicator (PRD 01 §4.8). Off-by-default + provenance + delete is the privacy-first posture (PRD 02 FR-40). |
| D20 | **Projects/Spaces are thin scoping containers, and there is one container hierarchy — not three.** A Project groups conversations and scopes the EXISTING wedge controls (default tier, retention, budget sub-cap, shared instructions); tags are orthogonal labels; archive is a lifecycle state; nested folders are deferred. Memory stays account-global at P1; a Project MAY associate a knowledge base, but RAG retrieval is out of scope (D25). | Holds the "chat product, not workspace/productivity suite" guardrail (§1/§3); deepens cost + privacy by scoping routing/retention/budget where work happens; avoids a confusing folders-vs-projects-vs-tags triple. Advances PRD 02 FR-21's *Projects* to P1 (Personas stay P2). |
| D21 | **History search gains transparency-native filters (served model/tier, per-message cost, date, tag, project) + Postgres full-text ranking; true vector/semantic search is deferred to P2 with RAG/pgvector.** | Filtering history by which-model-answered and what-it-cost is a capability only a transparency-first product can offer (we persist served-model + cost per message). Down-scoping "semantic" to FTS+filters avoids pulling pgvector forward. Resolves the PRD 01 §8 search-scope open question. |
| D22 | **Voice & generative-media I/O carry the full transparency spine.** Dictation (STT) + read-aloud (TTS) split out of the FR-33 P2 lump to P1; unified hands-free voice mode + in-thread image generation stay P2. Every audio/media interaction shows served model + cost + where data is processed; voice mode is conversational chat (no task-running-by-voice — an approval-gated tool still hits the visual `awaiting_approval` gate); a `modalities_out` registry field and (for image-gen) object storage are the named preconditions. | Read-aloud/dictation are high-a11y, low-infra wins mis-filed under a monolithic P2; the spine keeps "which model, what cost, where processed" true for non-text I/O; the guardrail keeps voice from drifting into a voice-assistant agent. |
| D23 | **The power-user/dev toolkit stays chat-anchored, never an agent/automation platform.** Prompt library + user-authored templates and customizable keyboard shortcuts ship P1; personas (labeled defaults — the served-model badge supersedes a persona's pinned label), artifacts/canvas (conversation-bound), code execution (a HITL tool in the shipped agent loop), and MCP **action** connectors (approval-gated, no background/scheduled automation) ship P2. P2 items are gated on real-provider tool wiring (the shipped loop is fake-provider v1). | Serves the primary power-user/dev beachhead while holding the §1/§3 guardrail: tools live inside a chat turn, customization is a labeled default not a lock, and transparency (the served badge) always wins over a pinned persona. |
| D24 | **Shipped (#143). Citations are the source-side of the transparency contract (PRD 07).** Inline `[n]` markers shipped as a render-layer upgrade (bound to `SourceItem.id`, clickable/hover-to-source, pending→resolved) over the already-shipped source-card list; a grounded-vs-ungrounded honesty rule, a provenance label (web/knowledge/connector), backend `requested`/provenance, and explicit share/export rules (web sources public-share-safe; private-knowledge citations redacted on public share) complete the contract. Gated behind web search (`SEARCH_BACKEND`, default `none`) like the search it annotates. | The transparency wedge has two halves — model+cost (owned) and citations (uncontracted). The backend already resolved ordinal sources and instructed the model to cite `[n]`; the gap was render-only. Defines the citation/privacy boundary before RAG ships so it isn't retrofitted (worker F owns the model/data-policy side; this owns the source side). |
| D25 | **File/document RAG (P2) is anchored to "a cited chat answer," not a knowledge-management suite.** Upload → chunk → embed (pgvector) → hybrid retrieve → CITE, every chunk a citation, retrieval cost metered, a registry-driven BYO-embedding option, and retrieval privacy/index-control shipped WITH RAG (not after). No folder hierarchies, doc editing, or permissions matrix. | The deferred P2 RAG line; gated on object storage + worker/queue + pgvector, with large-context "attach and ask" (FR-29) as the P1 bridge. Keeps retrieval inside the chat wedge (§1/§3); indexing creates a new PII store, so the delete/leak boundary is designed before indexing ships. |
| D26 | **Read-only data-source connectors (P2-stretch) are DATA/retrieval connectors — explicitly NOT action/MCP connectors (D23) and not a productivity-integration suite.** URL/sitemap + Drive/Notion-style READ-ONLY, least-privilege sync that populates citable retrieval corpora with a privacy manifest. | Lets answers cite a user's external knowledge while staying inside the chat wedge; read-only least-privilege scopes contain the security + KM-sprawl risk; keeps the data-vs-action connector boundary explicit in the doc. |
| D27 | **Shipped (#144). Cost transparency extends from per-message to longitudinal, with value-aware selection and proactive guardrails.** A user-facing spend-analytics surface shipped via `GET /api/account/spend` (daily / by-model / by-conversation buckets; month-to-date cumulative vs surviving-messages bases) over already-captured cost+attribution; a per-conversation budget cap (`preferences.per_conversation_budget_usd`, new migration `0019`) with 80%/100% soft-cap alerts layered over the shipped hard gate; and a label-only "Cheapest" badge (value-aware, **no silent model switching**). Surfaces label the cumulative-meter vs surviving-message cost basis. | The wedge we own is per-message transparency; its highest-leverage deepening is legible cumulative spend + actively helping users pay less — answering aggregator price-compression without a new routing brain or a silent downgrade. |
| D28 | **The subscription lifecycle and growth loops are completed on the shipped Stripe + USD-credit spine (D16).** Annual Pro + first-party proration/pause/cancel/dunning; referral credit grants + giftable credit packs as idempotent ledger entries with a first-class anti-abuse posture. Annual price is configurable (feeds the contested §9.1 band), not hardcoded. | The lifecycle is half-built (single price + Portal); annual lifts ARPU and cuts month-1 AI-tourist churn. Referrals are the CAC-efficient loop for the prosumer/dev beachhead, native to the shipped ledger. A credit faucet makes anti-abuse a launch requirement. |
| D29 | **Lightweight shared workspaces are the minimal prosumer teams on-ramp — explicitly NOT enterprise.** New workspace + owner/member membership + shared credit pool + shared Projects; BYOK stays personal; **excludes SSO/SAML/SCIM/SOC2/audit console/DPA/per-seat licensing** (those remain non-goals per §3). | Realizes the deferred-P2 teams persona (PRD 05 §3) as the smallest expandable slice without taking on the enterprise compliance burden the product has repeatedly declined; the workspace primitive is the seam future expansion hangs off. |
| D30 | **Shipped (#145). User-facing trust surfaces deepen the privacy/transparency wedge — distinct from an enterprise audit console (non-goal).** A data-access activity log via `GET /api/account/activity` (the `AuditEvent` trail — logins, exports, deletions, BYOK key use) plus a `GET /api/account/data-processing` rollup (which provider processed which message); a browsable model/data-policy directory via `GET /api/models/directory`; transparent moderation + `POST /api/account/moderation-appeal` (never a silent/generic block); and platform incident/status transparency (public `GET /api/status` + `/status` page + degraded-provider banner). Built on already-persisted data — **no migration**. | Trust is the product. These surface data Olune already persists (the write-only `AuditEvent` trail, per-message attribution, the `data_policy` registry, `Stream` telemetry) as user-visible proof — making the DeepSeek/China residency tradeoff (D11) auditable per message and applying "never silently downgrade" to moderation and platform health. |
| D31 | **Retention becomes per-conversation, with ephemeral/incognito chat and a scheduled purge; export goes multi-format.** Per-conversation retention override + durable `expires_at` + a scheduled purge job (the per-user purge fires only opportunistically today), sensitive-conversation handling, and PDF/.docx export honoring the §6.4 cost asymmetry (private retains cost, share-safe strips it). | Closes a real gap (retention is only eventually enforced on active users; temporary chat is in-process-only) and gives privacy-prosumers per-thread control; multi-format export is the deferred-P1 portability item done right against the transparency contract. |
| D32 | **Content-marking / provenance (EU AI Act Art. 50(2)) is designed-for now, built later.** Define the generated-media provenance contract on the typed-parts model (D7) now; build only when image generation ships AND after legal sign-off on the unsettled marking date; provenance travels with the media in both private export and public share (it is identity, not cost — never stripped). The shipped Art. 50(1) interaction disclosure is separate and already done. | The open compliance item (§10/§12). Designing the metadata contract now makes the obligation a metadata addition, not a refactor, when image-gen lands; the unsettled date stays a flagged legal gate, not a guessed launch date. |

> **D33–D40 — Agentic-mode pass (this brainstorm).** The decisions below scope a single **net-new, on-strategy P2 feature specced (not built)**: an **agentic mode** that extends the shipped single-agent tool loop (D15/D23) into an **in-turn orchestrator over bounded model subagents**. They deepen the power-user/dev wedge while holding the §1/§3 chat-product guardrail (chat-anchored, never an agent/automation platform). Full FR spec: **PRD 02 §4.6 (FR-26c–FR-26k)**; build sequence: **`docs/plans/01-agentic-mode.md`**; roadmap placement: **PRD 05 §4.5**. All are **P2, default-OFF, fake-provider-first** with real-provider wiring as the gating prereq.

| D33 | **Agentic mode = an orchestrator that spawns bounded model subagents, in-turn and chat-anchored — explicitly NOT background/scheduled automation.** It is the multi-agent *extension* of the shipped single-agent loop: an orchestrator that, within one assistant turn, fans out to N bounded subagents and synthesizes one answer, then terminates with the turn. **Extends D23.** P2. | "Every major model in one place" credibly includes hard, decomposable questions, but the §1/§3 guardrail forbids an agent/automation platform. Keeping the orchestrator strictly in-turn and chat-anchored deepens the dev/power-user beachhead without crossing into the agent-platform anti-goal D23 draws. |
| D34 | **Agentic mode ships default-OFF behind `AGENTIC_ENABLED`, itself gated by `TOOLS_ENABLED`.** With the flag off (the default) the orchestrator is never constructed and the stream is byte-identical to the shipped build; agentic cannot be active unless tools are. **Extends D15.** P2. | Matches the shipped default-off-behind-hard-flags discipline (`TOOLS_ENABLED` / `SEARCH_BACKEND` / `RESUMABLE_STREAMS_ENABLED`) so the highest-risk path (multi-agent fan-out, multiplied cost) is inert in prod until deliberately enabled; nesting under tools reflects that a subagent's whole point is the tool loop. |
| D35 | **Subagents reuse the shipped `run_agent_loop`; the orchestrator is a thin layer at the `_build_provider_iter()` seam.** No new loop engine — each subagent is an instance of the proven bounded loop (rounds, per-tool timeout, untrusted-output, HITL pause), and the orchestrator only adds fan-out/aggregate/bound/verify logic at the one existing handler seam. P2. | Reusing the proven loop keeps the new surface small and inherits every shipped guardrail; building at the single seam (`streaming/handler.py::_build_provider_iter()`) means the handler's accumulation, persistence, cancellation, and fallback are unchanged. |
| D36 | **Default stays a single ReAct loop; "Deep Research" parallel orchestration (planner→workers→reviewer) is opt-in per turn.** The common case has no fan-out cost; the parallel mode is an explicit composer toggle, never auto-engaged. P2. | Fan-out is materially pricier (D37), so the cheap single-loop default must hold; making Deep Research an opt-in toggle keeps cost visible and intentional, consistent with the no-surprise-cost ethos of the transparency wedge. |
| D37 | **Guardrails = a hard per-run USD cap + fan-out/recursion bounds + plan approval via the shipped `awaiting_approval` HITL gate.** The per-run cap reads the same per-message cost accounting the wedge already computes and degrades gracefully (partial synthesis) on breach. **Extends D27.** P2. | Multi-agent runs stack two cost multipliers (reasoning ~4–15× and multi-agent fan-out ~15×, `[verify-at-build]`), so an uncapped run is a margin landmine; reusing the shipped HITL terminal for plan approval adds no new pause primitive and keeps the human in the loop before high-cost fan-out. |
| D38 | **Agentic runs are transparent per subagent: subagent-scoped stream events + message parts + a live per-run cost meter + OTel `invoke_agent`/`execute_tool` spans.** Per-subagent served-model attribution + substitution codes apply — no silent downgrade inside a fan-out. P2. | The wedge is per-message transparency; a fan-out that hid which subagent did what (and what it cost) would break it exactly where cost is highest. Subagent-scoped parts are additive to the typed-parts union (D7); the OTel spans ride the shipped env-gated tracing path. |
| D39 | **Safety = transitive untrusted-output + bounded recursion + a verifier/self-consistency reviewer (N≈3–5).** A subagent's output is untrusted input to its parent/aggregator (fed back as structured data, never spliced into instructions), so a poisoned worker cannot inject up the tree; an optional reviewer/self-consistency pass is the cost↔quality dial. P2. | Multi-agent graphs widen the indirect-prompt-injection surface (SR-2 / OWASP LLM08); making untrust transitive across the whole graph (not just at the leaf tool) contains it, and a bounded verifier raises answer quality without escaping the per-run cap. |
| D40 | **Fake-provider orchestration v1 first; real-provider subagent wiring is the gating prereq.** The orchestrator ships and is proven end-to-end on the deterministic fake provider before any real-key environment enables it — exactly as the single agent loop shipped fake-provider-only. P2. | De-risks the highest-cost, highest-blast-radius path the same way tools shipped (fake-provider v1): the engine, bounds, transparency, and HITL are validated cheaply and deterministically before real tokens (and the multiplied multi-agent bill) are ever spent. |

**Implementation status — D11 (MVP deploy):** The production BE runs `PROVIDER_BACKEND=deepseek` as the main provider via the OpenAI-compatible binding to `https://api.deepseek.com`. The current tier registry uses `deepseek-v4-flash` for auto/fast/smart and `deepseek-v4-pro` for pro. The data-residency tradeoff (data processed in mainland China; jurisdictional bans) is documented and accepted, surfaced to users via the `data_policy` badge rather than gated out; privacy-sensitive users can select a Western route in the picker or supply their own key via BYOK. This is the settled provider decision, not a deviation.

**Implementation status — D10/D12/D13/D14 (shipped):** copy-on-branch ("Branch in new chat"), Mermaid rendering (lazy engine, `securityLevel:'strict'`), the attach affordance with vision gating, and both P0 Continue/Regenerate recovery and P1 resumable-stream replay have landed (#133, #135, #136). D13's attach/vision UI is present but **inert under the production DeepSeek backend** (non-multimodal); image interpretation requires the Anthropic backend.

**Implementation status — D24/D27/D30 (feature-expansion items now shipped):** three of the D19–D32 specs have landed on `main`. **D24 (#143):** inline `[n]` citation markers + the citation transparency contract (grounded/ungrounded honesty, provenance, share/export asymmetry, backend `requested`/provenance) over the existing source-card list, gated behind web search (`SEARCH_BACKEND`, default `none`). **D27 (#144):** spend analytics (`GET /api/account/spend`), a per-conversation budget cap (migration `0019`, `preferences.per_conversation_budget_usd`) with 80%/100% soft-cap alerts over the shipped hard gate, and a label-only "Cheapest" badge — **no silent model switching**. **D30 (#145):** the data-access activity log (`GET /api/account/activity`), a data-processing rollup (`GET /api/account/data-processing`), a model directory (`GET /api/models/directory`), transparent moderation + appeal (`POST /api/account/moderation-appeal`), and public status (`GET /api/status` + `/status` page + degraded banner) — built on already-persisted data with **no migration**. The remaining D19–D32 decisions stay specced (not built).

---

## 12. Open questions

Each PRD carries its own open-questions section; the cross-cutting product/business ones (Pro price point, BYOK monetization, free-tier caps, EU launch timing vs AI-Act dates, secondary-persona timing, RAG-pull-forward, native trigger, ads-at-scale, minors/companion personas) are in **PRD 05 §9**. Remaining technical spikes (virtualization × streaming, iOS keyboard, custom-session auth expansion, Postgres operations, resumable-stream hardening) are in **PRD 03 §9 / PRD 04 §9**.

**Contested — FLAGGED, not decided here** (per product-owner policy; needs the named owner / legal before lock):
- **Exact Pro price (~$15–20 vs T3 Chat's $8 anchor).** Our band is 2–2.5× the $8 multi-model floor — justify the premium (privacy + a11y + mobile-web + true cost transparency) or reprice (PRD 05 §9.1).
- **EU AI Act content-marking date (Aug 2 vs Dec 2 2026),** clouded by a May-2026 Council/Parliament provisional amendment reshuffling deadlines — **needs legal sign-off** before EU-launch scope is locked (PRD 04 §9 / PRD 05 §7.5/§9.6).
- **Whether we serve minors / offer companion-style personas** — triggers CA SB 243 + surviving child-safety carve-outs (crisis protocols, break reminders, minor protections); needs explicit product + legal decision (PRD 05 §7.5/§9.11).

**Newly opened by the feature-expansion pass (D19–D32) — flagged for the owner:**
- **Project-scoped vs account-global memory.** D19/D20 ship memory account-global at P1; project-scoped memory is a flagged P2 refinement — decide before Projects+memory compose (PRD 01 §4.8 / §4.5).
- **RAG embedding-route default + hosting (D25).** Platform-cheap default embeddings vs BYO-embedding, and where embeddings + source objects live (data-residency posture) — gated on the object-storage + worker/queue decision (PRD 04 §5.4/§5.6) and the registry-driven embedding model (no hardcoding).
- **Workspace ↔ Stripe-customer mapping + pooled-credit accounting (D29).** How a shared credit pool maps to a single billing customer and composes with operator caps — needs the data-model + billing decision before prosumer workspaces ship (PRD 04 §6 / PRD 05 §5.1).
- **Annual Pro price point (D28).** Feeds the already-contested Pro-price band above; the annual discount is configurable, not yet set (PRD 05 §9.1).

**Newly resolved by the implementation/review pass:** the shipped foundation is Next.js FE + FastAPI/Fly backend, custom signed-cookie auth, and SQLAlchemy/Alembic on Neon; free-tier default and main provider **resolved to DeepSeek** (current implementation: `deepseek-v4-flash` / `deepseek-v4-pro` via the OpenAI-compatible binding — see D11), with Anthropic as an alternate backend and Western routes selectable in the picker; **metered Pro + P0 metered-overage primitive** confirmed (PRD 05 §9.4). See the decisions log (§11, D6–D11). Shipped ahead of/within phase: structured outputs, per-turn reasoning-effort override, provider fallback, public share links, GDPR export/delete, analytics, custom instructions + retention + monthly budget cap (all P0); tools/HITL, web search, resumable replay, Stripe billing, and safety preflight behind default-off flags (D15–D18).

**Resolved by the feature-expansion pass (D19–D32):** the **search-scope** question (PRD 01 §8) resolves toward Postgres FTS + transparency-native filters, with vector/semantic deferred to P2 alongside RAG (D21); the **provider status-page** strategy (PRD 08 §13 #4) is now resolved *and shipped* as a first-party `GET /api/status` (+ `/status` page + degraded banner) derived from `Stream` telemetry rather than a third-party vendor (D30, #145); and **moderation-appeal** timing (PRD 08 §13 #5) shipped the user-facing transparent-block + appeal-capture (`POST /api/account/moderation-appeal`), with operator review tooling still at P2 (D30, #145).

---

## 13. References

- Workstream PRDs: [01](01-core-chat-experience.md) · [02](02-ai-capabilities.md) · [03](03-mobile-cross-platform.md) · [04](04-technical-architecture.md) · [05](05-roadmap-monetization-metrics.md) · [06](06-design-system-visual-spec.md) · [07](07-transparency-contract.md) · [08](08-error-and-limit-states.md)
- **Traceability:** competitive/research findings are summarized and mapped in PRD 05 §10.1. Original `research-*.md` files are not checked into this repo; PRD file + section references are authoritative for build/review.
