# PRD 01 — Core Chat Experience (Web + Mobile-Web)

**Owner:** Product (Core Conversational Surface)
**Status:** Draft for build
**Date:** 2026-05-27
**Related PRDs:** 00 Product Overview · 02 AI Capabilities & Model Integration · 03 Mobile / Responsive · 04 Architecture · 05 Roadmap / Monetization.

---

## 1. Summary & purpose

This PRD specifies the **core conversational experience** — the chat surface itself — for a transparent, multi-model, cost-leading AI chat product on web and mobile-web (mobile-web first). It covers everything a user sees and touches while having a conversation: streaming responses, the reasoning panel, the composer, rich content rendering, conversation management, message actions, onboarding, basic settings, sharing/export, keyboard shortcuts, and the accessibility baseline.

The product thesis: **incumbents win on breadth; we win on a fast, polished, accessible core.** Rendering/streaming fidelity, composer ergonomics, accessibility, and mobile-web polish are the levers we are deliberately over-investing in. A janky renderer or a laggy stream reads as "low quality" instantly; a crisp one reads as premium. This document is written so a senior engineer can build the MVP from it.

Out of scope here (owned elsewhere): model internals/routing/cost accounting (PRD 02), mobile-specific composer/keyboard/gesture behavior (PRD 03), streaming transport and persistence architecture (PRD 04), and organizational primitives like Projects/Spaces (post-MVP, separate spec).

---

## 2. Goals & non-goals

### Goals
- Ship a conversational surface that **feels faster and more polished** than incumbents on the basics: first-token latency perception, smooth streaming, flawless markdown/code/math rendering.
- Make **transparency a first-class UI value**: show which model answered each message; expose (but don't force) reasoning.
- Deliver an **accessibility baseline that beats the leaders** (who have documented gaps) — labeled controls, a correct streaming announce model (status-only polite region; see §5.7), full keyboard operability, announced status.
- Provide a **simple default mode** so casual users aren't excluded, while giving power users/developers shortcuts, a command palette, and dense controls.
- Be **excellent on mobile-web**, not a shrunk-down desktop UI (detailed in PRD 03; this PRD sets responsive expectations).

### Non-goals (this PRD / this phase)
- Heavy parallel **model comparison** (run N models side-by-side) — later.
- **Explicit copy-on-branch ("branch from here / branch in new chat")** — now **P1** (was P2), designed as a low-risk copy model, never an in-thread tree (see §4.6, §8). **In-thread alternate-response trees** remain P2+.
- **Projects/Spaces, folders, tagging, pin/archive** — post-MVP organizational layer (now specced as a fast-follow — PRD 05 §4.5 / PRD 00 D20–D21; pin already shipped).
- **Voice mode, image generation, code execution / data analysis** — owned by PRD 02, later phases.
- **Memory** (persistent cross-chat facts) — fast-follow (P1); we only ship the *custom instructions* entry point in MVP. Note: memory is now a *free baseline* across incumbents, so our P1 spec leads with the **transparency layer** (ledger + "memory used here" + import), not the feature itself — see §4.8.
- Full multi-format export (PDF/.docx) — MVP ships copy-as-markdown + share link only.
- **Proactive / ambient briefings** (ChatGPT Pulse / Gemini Daily Brief style overnight digests) — **deliberate non-goal**. They are connector/agent-heavy and require reading the user's mail/calendar, which cuts against our privacy-first posture. Listed here so the team isn't surprised when incumbents lean in.

---

## 3. Target users & key user stories

**Priority persona (MVP):** power users / developers. **Secondary:** privacy-conscious prosumers. **Constraint:** keep a simple default so casual users succeed without learning anything.

Key user stories:

1. **As a developer**, I paste a long error log and ask for a fix, so I want code blocks to render with syntax highlighting and a one-click copy, and I want to keep the partially-streamed answer if I hit Stop early.
2. **As a power user**, I want `Cmd/Ctrl+K` to jump to any past chat or start a new one without touching the mouse, and a discoverable shortcut list.
3. **As a privacy-conscious prosumer**, I want to know exactly which model produced an answer, to see its reasoning if I choose, and to find data controls without hunting.
4. **As a casual user**, I land on an empty state with a friendly greeting and 3–4 example prompt cards so I immediately know what I can ask.
5. **As a screen-reader user**, I want every icon button labeled, generation status announced, and the streamed answer readable without fighting focus.
6. **As any user on my phone**, I want a thumb-friendly composer, a Stop button that's always reachable, and tables/code that don't break the layout.
7. **As a user who got a great answer**, I want to copy it as markdown or share an unlisted link in two clicks.

---

## 4. Functional requirements

Priority tags: **[P0/MVP]** must ship to be credible · **[P1]** fast-follow · **[P2]** later/differentiator. Acceptance criteria (AC) given where load-bearing.

### 4.1 Streaming response
- **[P0]** Token/word streaming renders incrementally as chunks arrive (SSE; transport per PRD 04).
  - *AC:* Visible text updates within ~50ms of each chunk; no full-message re-layout flicker per chunk.
- **[P0]** **Pre-first-token indicator**: animated typing/skeleton indicator shown from send until first content token.
  - *AC:* Indicator appears <150ms after send; replaced by streamed content on first token; never shown simultaneously with content.
- **[P0]** **Stop control that preserves partial output**: Stop aborts the stream (client `AbortController`, cancellation propagated per PRD 04) and **keeps everything streamed so far** as a normal, actionable assistant message.
  - *AC:* After Stop, partial message is persisted, copy/regenerate available, and a subtle "stopped" marker is shown.
- **[P0]** **Tool/status lines**: when the backend emits intermediate steps (e.g., "Searching the web…", "Running analysis…"), render them as status lines above/within the forming message. (Step payloads defined in PRD 02.) These are **not throwaway text**: a status line is the streaming form of a **tool-call part** (§4.4) and must be persistable, so designing them as transient-only is prohibited.
- **[P1 — shipped: UI present, backend gated off by default]** **Tool-call parts with human-in-the-loop (HITL) approval** (design now, lands with tools). Status lines become **persistable, expandable tool-call parts** that show the tool name, inputs, and output/result, with **errors/retries** on a step. When a step requires consent (a tool reads user data or calls an external service), the part presents **approve / deny / approve-with-edits** controls and **pauses** generation until the user acts. Target **MCP** as the interop shape (capability owned by PRD 02; AI SDK v6 `Agent` / `needsApproval` wiring owned by PRD 04 — reference both). This consent gate is acceptance-relevant for our privacy-first posture.
  - **Shipped:** tool-call/tool-result parts render with a status icon + pill (pending/running/complete/failed/cancelled/awaiting-approval) and, on an approval-gated step, surface inline **approve / deny** controls that pause the turn on that step (`tool-part.tsx`; `chat-thread.tsx` `handleToolDecision` rides a follow-up `toolApproval` resume POST). The backend tool-calling loop is behind **`TOOLS_ENABLED` (default `False`, `api/app/config.py`)**, so the loop and pause state are off until that flag is set. **Approve-with-edits is NOT shipped** — the controls are approve/deny only.
  - *AC (P1):* an approval-gated tool step blocks the stream, surfaces inputs/context for review, and on deny/edit feeds the decision back without losing prior streamed content.
- **[P1]** Resume an interrupted stream after a transient disconnect (resumable streams per PRD 04) without losing already-rendered tokens.
- **[P2 — SHIPPED\* behind `AGENTIC_ENABLED`; partial on per-subagent attribution] Agentic-mode subagent activity + live progress.** When **agentic mode** (PRD 02 §4.6 FR-26c–FR-26k; PRD 00 §11 D33–D40) fans a turn out to bounded subagents, the thread renders **per-subagent activity** — each worker's label, status, and intermediate output as **subagent-scoped parts** (additive to the typed-part union, §4.4) — plus a **live per-run cost meter** rolling up subagent costs as they complete (`subagent-panel.tsx`, `assistant-message.tsx`). The opt-in **"Deep Research" toggle** is a composer mode toggle (§4.3, peer of web-search / reasoning-effort) gated on bootstrap `agenticEnabled`, and **plan approval reuses the shipped tool-approval pause UI** (`tool-part.tsx`, §4.1 HITL) at the orchestration boundary.
  - **Shipped:** `SubagentPanel` (per-worker rows + run-cost meter), Deep Research toggle (`model-mode-picker.tsx`), plan-approval card, subagent-grouped reload layout.
  - **Not shipped:** per-subagent served-model badge + substitution callout (turn-level `AttributionRow` only — PRD 07 §4.4); explicit high-cost composer hint (FR-26f); PRD 08 partial-synthesis warning chip; share-view subagent rendering.

### 4.2 Reasoning / "thinking" panel
- **[P0]** A **collapsible reasoning panel** attached to an assistant message, rendered only when the model emits reasoning/summary content (provider-shaped; may be a summary only — see PRD 02 §3).
  - **Auto-open** with a shimmer + label ("Thinking…") while reasoning streams.
  - On completion, show **"Thought for Xs"** and **auto-collapse**.
  - User can **expand/collapse** any time; expansion state is per-message and remembered for the session.
  - *AC:* If no reasoning content is emitted, no panel/affordance appears (no empty panel). We never fabricate or store reasoning the provider hides.
- **[P1 — shipped]** A per-conversation or per-message **"reasoning effort"/extended-thinking** toggle surfaced in the UI (mapping to provider knobs lives in PRD 02). The toggle **must surface a cost/latency hint** (e.g., a relative "higher cost / slower" indicator) so users understand the trade-off — reasoning tokens drive the large per-turn cost variance the product warns about, so a "max reasoning" control with no visible cost consequence would undercut our transparency wedge. Cross-ref **PRD 02** reasoning-token accounting for the underlying numbers.
  - **Shipped:** a per-turn effort control (Auto / Minimal / Standard / Extended) lives in the model picker, each non-Auto option showing a relative **`Cost … · Latency …` hint** so a "max reasoning" pick can't be made without seeing the trade-off (`lib/reasoning-efforts.ts`, `model-mode-picker.tsx`). When the served provider ignores effort the rows render **disabled with a one-line note** ("This model ignores reasoning effort.") rather than an error — meeting the cost-consequence requirement.

### 4.3 Message composer
- **[P0]** Multiline textarea: **Enter sends, Shift+Enter inserts newline** (desktop). Auto-grows to a max height then scrolls. (Mobile send/keyboard behavior: PRD 03.)
- **[P0]** **Send button morphs into Stop** during generation; same position.
- **[P1 — partially shipped]** **Attachments**: paperclip/"+" menu to attach images and files; **paste image from clipboard**; **drag-and-drop** files onto the composer with a drop overlay. (Accepted types/limits and what the model does with them: PRD 02.) **Deferred to P1 with vision/PDF understanding** — the lean text-core MVP is text-only, so there is no attach affordance at launch.
  - **Shipped:** a paperclip opens the file picker (image / PDF / text; ≤4 files, ≤5MB each; removable, labeled chips) and a vision gate auto-removes image attachments on a tier that accepts files but can't interpret images, keeping PDFs/text (`composer.tsx`). The paperclip is shown **only when the active tier reports `supportsAttachments`** — so it is **inert on the prod DeepSeek backend** (`supports_attachments=True` but `supports_vision=False`, so images are rejected) and lights up fully only under the **Anthropic** backend (vision + attachments True).
  - **NOT shipped:** clipboard paste-image, drag-and-drop with a drop overlay, and re-attach-recent (see §4.3 below). Attachment chips that exist are removable and labeled.
  - *AC (P1):* Pasting an image inserts a thumbnail chip; drag-drop shows a highlighted drop zone; each attachment chip is removable and labeled.
- **[P0]** Plain-text **paste** into the composer works at MVP (e.g., pasting a long error log); only *image/file* attachment is deferred.
- **[P0]** **Model picker** lives in the composer (compact control adjacent to send) **and** is reflected in the thread header. Presents **capability tiers** (e.g., Fast / Smart / Pro), not raw model IDs, driven by the model registry (PRD 02 owns tiers/registry). Selection can change mid-thread.
- **[P1] Value-aware model picker + "cheapest capable route" (cost wedge made actionable).** Turn the picker from "we show the cost" into "we help you pay less" without re-speccing the routing brain. Cite **D27**.
  - *Wedge:* cost leadership + multi-model choice + transparency — price/latency/quality made comparable at the point of decision. *Value:* the picker already receives per-tier `list_price_in/out_per_m`, `speed_hint`, `cost_hint`, `data_policy`, and `provider_options` from bootstrap, but the user still picks blind on *value* (no at-a-glance comparison, no overpay nudge). This is the everyday-selection complement to the shipped 2-up compare view (which is for exploration).
  - *Behavior:* (a) **value chips** on each tier/provider-option row — a comparable triplet of relative **price** ($/$$/$$$ derived from list prices), relative **latency** (measured per-model TTFT where instrumented, else `speed_hint`), and a **quality/recommendation** hint (the user's own thumbs-up rate where votes exist, else tier class); the `data_policy` badge stays. (b) An opt-in **"cheapest capable route" suggestion** — an inline, dismissible hint when a user repeatedly runs simple prompts on an expensive tier ("Auto would route this ~X% cheaper"), reusing the existing `auto` router classification (PRD 02 owns the heuristic); it **never auto-switches** (no silent downgrade — §4.6, PRD 00 §7), only suggests, and is suppressed once the user has manually overridden Auto. (c) **Saved comparisons** — pin a {prompt, tiers} set from the 2-up view to re-run (a power-user workflow on top of compare). (d) Surface the per-tier **pre-send estimate** already computed for non-Auto tiers as a "~$ per typical message" line.
  - *AC:* picker rows render price/latency/quality on one comparable scale sourced from registry prices + measured latency + the user's own votes (no hardcoded prices — PRD 02 §5); the suggestion is opt-in, never mutates the selection without an explicit action, and reuses the auto-router (no second routing brain); saved comparisons persist per user and store no provider secrets; quality hints fall back to tier-class label (never a fabricated score) when the user has no vote history; **BYOK routes show "$0 platform markup"** rather than a list price.
  - *Data/transparency/privacy/cost:* reads existing registry pricing + attribution/vote data; saved comparisons are a small per-user table; no new provider spend. It **consumes** `provider_options` + the auto-router and **does not alter** the PRD 07 per-message cost/attribution contract. *Dependencies:* shipped tier registry + `provider_options`, 2-up compare, auto-router; PRD 02 §4.2 (routing internals); PRD 07 (contract, surfaced not changed); E1 usage surface (§4.8 — supplies the quality/value data). *Mobile-web + a11y:* the triplet must be readable on a narrow picker — text + icon, **never color-only** for price/latency tiers; the suggestion is a non-modal, dismissible hint (no gesture-only dismissal); saved comparisons reachable from the `Cmd/Ctrl+K` palette. *Phase:* **P1.**
- **[P1 — slash popover shipped (client-static); user-authored library net-new]** **Native slash commands (`/`) + prompt library** popover for quick actions/prompt templates. **Reframed from "risky/unproven" to a committed P1 differentiator:** no consumer chat ships native slash commands today, so users bolt browser extensions (Prompster/Slashprompt) onto incumbents — an underserved need our dev/power-user persona already hacks around.
  - **Shipped:** a keyboard-navigable `/`-triggered popover (`slash-commands-popover.tsx`) that filters a registry and prefills the composer with the selected prompt. The registry is a **static client-side list** (`MOCK_COMMANDS` in `lib/mock-data.ts`) — this is the composer-UX prefill, **not yet a server-backed or user-editable prompt library**.
  - **[P1] Prompt library & user-authored templates (net-new layer over the shipped prefill). Cite D23.** Turn the demo affordance into a real power-user accelerator: **user-authored, persisted, variable-bearing templates** surfaced in the *same* `/` popover and the `Cmd/Ctrl+K` palette (§4.9). This fills the existing "prompt library" roadmap line and is the lowest-cost, highest-frequency feature for the dev/power-user beachhead. *Wedge:* core chat loop (composer ergonomics — the most-used surface) + the power-user persona who re-issue the same scaffolded prompts dozens of times a day.
    - *Behavior:* a **prompt** = `{ name (slash trigger), title, description, body, variables[], icon, visibility }`; `body` may carry `{{placeholder}}` tokens, each mapping to a declared variable `{ key, label, default?, required? }`. **Authoring** via (a) a Settings → Prompts manager (create/edit/duplicate/delete, search, drag-reorder) and (b) a one-tap "Save this as a template" on any sent user message; an empty state ships the current 8 built-ins as **forkable seed templates** so the curated starter set isn't lost. **Invocation** — typing `/` opens the popover now backed by **built-ins ∪ user prompts** (deduped by name; a user prompt shadows a built-in of the same name with a subtle "custom" chip); a zero-variable prompt prefills body + caret exactly as today, while a prompt **with** variables opens a compact inline **fill-in step** (a small form above the composer, Tab between fields, Enter to insert the interpolated body). **Transparency-preserving (load-bearing):** a template is **pure composer prefill** — it never silently changes model/tier/tools and produces ordinary user text; the assistant turn's attribution is unaffected. (A template that *also* pins a model is a Persona — PRD 02 §4.13, P2 — not a prompt; this keeps the two concepts clean.) Variable interpolation is **plain string substitution only** — `{{…}}` is not an expression language and never executes (no injection surface beyond ordinary user text).
    - *AC:* a signed-in user can create a prompt with a name, body, and ≥1 `{{variable}}` that persists across reload and devices (server-backed, not localStorage); typing `/<name>` ranks the user prompt alongside built-ins, and selecting a zero-variable one prefills body + caret-at-end (matching current trailing-space behavior); a variable prompt shows one field per declared variable, blocks insertion until required fields are filled, and on confirm replaces `{{key}}` tokens and inserts editable text; a name collision with a built-in resolves to the **user** prompt (marked custom) and deleting it restores the built-in; anonymous users get the built-in seeds read-only and "Save as prompt" prompts them to link an account before any server write (mirrors the BYOK guest gate); templates never alter the served model/tier/cost (verified vs. typing the same text by hand).
    - *Data/transparency/privacy/cost:* new `prompt_template` table keyed to `user_id`, owned by **PRD 04 §6**, served on `bootstrap` alongside the tier payload so the popover hydrates on load; write-rate-limited (reuse the `RATE_LIMIT_*` pattern). Distinct from `PromptSuggestion` (bootstrap-served onboarding empty-state seeds — §4.7) which is **not** merged with the library. **Privacy:** template bodies are user content — included in `GET /api/account/export` and erased on account delete. **Cost:** zero provider cost (pure prefill); a tiny CRUD surface. *Dependencies:* the shipped `SlashCommand`/popover + palette (extend the type with `variables?`); PRD 04 data model + bootstrap payload; no provider/model dependency. *Mobile-web + a11y:* the `/` popover already carries 44pt rows + `aria-activedescendant`/listbox semantics; the variable fill-in form keeps keyboard-only operability (Tab/Enter), `aria-label` per field, visible focus, and on mobile stacks above the composer respecting the visual-viewport keyboard inset; the authoring manager is a focus-trapped settings dialog. *Phase:* **P1** (sharing/import of prompts is P1-late or P2).
- **[P1]** **Re-attach recently used files** in the attach menu (lands with attachments) — a low-cost composer win for repeat-context workflows. *(Not shipped — the attach picker has no recents.)*
- **[P1] Dictation (speech-to-text) in the composer. Cite D22.** A **mic button** adjacent to Send lets users *speak* a prompt instead of thumb-typing — a mobile-web and motor-accessibility win. The mobile-input/permissions UX is owned by **PRD 03 §4.7** and the STT capability/registry/pricing/`data_policy` by **PRD 02 §4.8** (the same spine read-aloud uses, §4.6); this PRD anchors the composer affordance. Net-new — no `SpeechRecognition`/`MediaRecorder` dictation code exists today.
  - *Wedge:* mobile-web-first polish + accessibility (a genuine aid for motor-impaired users) + privacy as a configurable posture.
  - *Behavior:* tapping the mic starts capture; tapping again (or a VAD silence timeout) stops. **Two-track engine, capability-detected:** Track A = Web Speech API (`SpeechRecognition`) streaming **interim results** live into the textarea (zero-infra default where supported); Track B = server/BYOK STT (audio via `MediaRecorder` → a BE STT route per PRD 02 §4.8) — the only path that works in an **installed iOS PWA** (Track A is broken there) and the only path a privacy-sensitive user can route to a no-train provider. **Editable transcript before send is mandatory** — dictation **never auto-sends**; recognized text lands in the normal composer textarea to edit/append/discard, then send with the normal Send control (auto-send is an accessibility and trust failure). **First-use disclosure** — before the first capture, an in-UI disclosure ("Dictation sends audio to <route> to transcribe it") naming the actual route's `data_policy` (PRD 02 §4.8); never described as on-device (Web Speech is **server-side**). **Graceful absence** — the mic is hidden/disabled where unsupported (Firefox; installed iOS PWA on Track A) unless a server STT route is configured: no silently-dead control. **Recording-state affordance** — a clear animated "listening" indicator with an elapsed timer + visible Stop, honoring `prefers-reduced-motion`.
  - *AC:* tapping the mic with a supported engine streams interim tokens into the composer and the text is fully editable before any send; dictation output is never auto-sent (sending requires the existing Send affordance, PRD 03 §4.3); on a context with no available STT engine the mic is absent or disabled with an accessible explanation (never appears then fails on tap); the first capture in a session is gated by a disclosure naming the route + posture, and declining cancels capture leaving the composer untouched; in an installed iOS PWA with Track A unavailable the mic is hidden or routes to Track B; the mic/indicator/Stop are keyboard-operable and screen-reader-labeled, with the listening state announced once via the §5.7 polite status region (not per partial).
  - *Data/transparency/privacy/cost:* audio bytes are **transient** on Track B (captured for the request, posted to the STT route, never persisted — mirrors the attachment transient-only discipline); the route's `data_residency`/`trains_on_data` (PRD 02 §4.8) drive the disclosure copy ("where is my voice processed?"); STT cost (per-minute or per-token) accrues to the usage meter for platform-key STT, while BYOK-STT bills the user's key with no markup (PRD 07 §6.3). *Dependencies:* **PRD 02 §4.8** (STT registry binding + audio pricing + `data_policy`); **PRD 03 §4.3/§4.7/§4.8** (composer + mobile mic permissions/a11y); read-aloud (§4.6, shares the two-track/disclosure pattern); no dependency on attachments/vision. *Mobile-web + a11y:* primary value is mobile-web; mic tap target ≥44–48px; Track A breaks in an installed iOS PWA (handle per above); a genuine motor-accessibility aid that extends the §5.7 accessibility baseline (a closed, confirmable voice-command-of-the-app layer over the same STT engine is a P2 a11y extension, owned in PRD 03 §4.8). *Phase:* **P1** (this is the missing functional spec, not a new phase — PRD 03 §4.7 already tags voice/STT P1).
- **[P1 — shipped]** Inline **mode toggles** in composer (e.g., web search on/off, reasoning effort) once those capabilities land (PRD 02). The inline-toggle pattern is increasingly the *primary* mode-switch UI (vs. a dropdown) and is the intended home for our P1 web-search/reasoning controls.
  - **Shipped:** web-search, reasoning-effort, and JSON-output toggles live in the model picker (`model-mode-picker.tsx`). The web-search toggle is shown **only on tiers that report `supportsWebSearch`**, and the search backend itself is **`SEARCH_BACKEND` (default `none`, `api/app/config.py`)** — the provider never advertises the tool until Tavily is configured.
- **[P2 — SHIPPED\* behind `AGENTIC_ENABLED`] "Deep Research" mode toggle (agentic mode).** A **peer inline toggle** alongside web-search / reasoning-effort that opts a turn into bounded-subagent fan-out, shown **only when bootstrap advertises `agenticEnabled`** (`AGENTIC_ENABLED && TOOLS_ENABLED`) — no silently-dead control. Enabling it renders per-subagent activity + a live per-run cost meter in the thread and reuses the tool-approval pause for plan approval (§4.1). **Not shipped:** explicit "high-cost mode" hint before send (FR-26f).
- **Non-goal (MVP):** token/character counters in the composer (hidden complexity; reconsider only if user research demands it).

### 4.4 Rich content rendering (the single biggest perceived-quality lever — see §5.4)
- **[P0 — message model]** A message is an **ordered list of typed parts** (`text | reasoning | status | sources | attachment | tool_call | tool_result` as-built per PRD 04 §5.3 / PRD 00 §11 D7), **not** a single markdown string. **Ownership:** the data-model schema for typed parts is owned by **PRD 04** (reference it; a separate worker keeps the schema authoritative). This PRD owns the **rendering contract**: the renderer iterates an ordered part list and dispatches by type so new part types are *additive*, never a rewrite. **At P0 the renderer renders text, code, and reasoning parts;** tool-call/tool-result, sources, and status parts render at **P1** (they arrive with tools/search/vision). This is the cheapest high-leverage architectural decision in the doc — modeling a message as one blob would force a painful refactor when P1 features land.
- **[P2 — SHIPPED\* behind `AGENTIC_ENABLED`] Subagent grouping part** is an **additive** member of the typed-part union (`SubagentPart` in PRD 04 / `schemas/message.py`): the renderer dispatches a subagent-scoped group that nests its worker's parts under a labeled, collapsible activity header (`SubagentPanel` in `assistant-message.tsx`), preserving the ordered-part contract — the chat-surface side of agentic-mode rendering (§4.1; event/part contract owned by PRD 02 §4.6 FR-26h). Share view does not yet render this grouping.
- **[P0]** **Streaming-safe markdown** (CommonMark + GFM) for text parts. Must render *mid-token* content gracefully — unclosed code fences, half-written tables, dangling emphasis must not flicker, break layout, or dump raw markdown. Adapt a battle-tested streaming renderer (Streamdown-style); do not roll our own naive parser.
- **[P0]** **Code blocks**: syntax highlighting (Shiki/Prism-class), language label, **copy button**. *AC:* copy puts exact source on clipboard; highlighting applies progressively during stream without re-flicker.
- **[P0]** **Math** via **KaTeX**: inline `$…$` and block `$$…$$`.
- **[P0]** **GFM tables**, horizontally scrollable on narrow viewports.
- **[P0]** **Images**: the renderer supports inline images with alt text (cheap markdown capability). User-attached and model-generated images are **exercised at P1** when attachments/vision land (§4.3).
- **[P0]** **Collapsible long content**: very long code blocks / responses get a "show more" / collapse affordance to keep the thread scannable.
- **[P1 — shipped]** **Mermaid diagrams** with a fullscreen view.
  - **Shipped:** a lazy-imported Streamdown diagram plugin renders ```mermaid fences (the ~500KB engine is kept out of the initial chunk and off SSR), pinned to `securityLevel: 'strict'` against untrusted model output, theme-aware (re-themes on light/dark), with a raw-source fallback on parse failure and Streamdown's fullscreen affordance (`markdown-renderer.tsx`).
- **[P1]** **Interactive blocks as a rendering baseline (not "Artifacts").** The renderer **hosts typed interactive blocks** — e.g., a chart spec rendered client-side with sliders, an adjustable/rotatable diagram — delivered as an `interactive-block` part (§4.4 message model). Inline interactivity is now expected response-surface functionality across the majors, *distinct* from a full Canvas/Artifacts panel (§4.12). **P0 ships a static-chart renderer; P1 adds interactivity.** Do not build a 3D engine for MVP — the requirement is only that the renderer/schema can host these blocks additively. Full generative/MCP-returned UI components remain **P2** (§4.12).
- **[P1]** **Answer-first / progressive-disclosure layout** (renderer/prompt experiment). Lead with key info / a TL;DR, then progressive detail below — distinct from the reasoning panel; an answer-*layout* convention our answer-hungry dev persona tends to prefer. Cheap, high-perceived-quality; validate with the persona.
- **[P1]** Code block **download** button and optional line numbers.
  - *Status:* copy + progressive syntax highlighting ship at **P0** (§4.4 code blocks), but the **download control is wired-but-disabled** — the renderer passes `controls={{ code: { download: false } }}` to Streamdown (`markdown-renderer.tsx`), so the button is suppressed. Line numbers remain deferred.
- **[P0 security]** Sanitize all rendered HTML/markdown via **`rehype-harden`-class allowlist hardening** (the mechanism shipped in Streamdown — see §5.4); safe link handling (`rel="noopener noreferrer"`, no script injection). Treat model output as untrusted (cross-ref PRD 02 §9).

### 4.5 Conversation management
- **[P0]** **New chat** button (sidebar top) + shortcut.
- **[P0]** **History sidebar**, reverse-chronological, **grouped by time** ("Today / Yesterday / Previous 7 days / older").
- **[P0]** **Rename** and **delete** a conversation via hover/overflow (`···`) menu; delete asks for confirmation.
- **[P0]** **Search** across conversation titles and content, surfaced via the command palette (§4.9) and a sidebar search field.
  - *AC:* Search returns matching chats with a title match and, where the index supports it, a content snippet; selecting one opens it scrolled to context. (Title-only vs full-text depends on PRD 04's index — see §8.)
  - **[P1] Advanced history search — transparency-native filters + full-text ranking. Cite D21.** Extends the shipped substring search (today LIKE over title + message text, returning a `matchSnippet`; no filters, no FTS) into the differentiated layer. *Wedge:* transparency + core chat loop — filtering history by **which model answered** and **what it cost** is a capability only a transparency-first product can offer, because only we persist served-model (`message.attribution`) + `cost_usd` per message. The killer query: *"every chat where Pro answered and it cost over $0.10"* or *"my DeepSeek chats from last month tagged research."*
    - *Behavior:* **filter chips** on the search surface (sidebar + `Cmd/Ctrl+K` palette) — served model/tier, date range, cost (per-conversation total or per-message threshold), **tag** (org v2 below), **project** (below), and archived inclusion; results show the existing `matchSnippet` plus the matched conversation's model/cost badges. **Text-matching upgrade path:** keep the portable LIKE matcher as the baseline; **on Postgres (prod) add full-text search** (`to_tsvector`/`tsquery`) for ranked relevance + better snippets, dialect-guarded to fall back to LIKE on SQLite so the dual-dialect contract holds. **True vector/semantic search is explicitly out of scope here** — it needs pgvector (PRD 04 reserves it P2) and lands with RAG (PRD 02 retrieval); "semantic" is down-scoped to ranked-FTS + structured filters for P1 to avoid pulling pgvector forward.
    - *AC:* a served-tier filter returns only conversations with ≥1 assistant message served by that tier; a cost filter (e.g. "> $0.10 total") returns only conversations whose summed `cost_usd` exceeds it; date/tag/project filters compose with text + model + cost (AND semantics); on Postgres a multi-word query returns relevance-ranked results with snippets while SQLite returns correct unranked LIKE matches (both pass the search suite); filters are keyboard-operable and the palette stays screen-reader navigable (§4.9/§5.7).
    - *Data/transparency/privacy/cost:* model/tier + per-message cost already exist; cost-per-conversation is a sum over messages; FTS adds a Postgres `tsvector` (expression index / generated column) behind a dialect guard (PRD 04). Search stays per-user-scoped (no new exposure); negligible cost (DB query only, no provider calls). *Dependencies:* `search_for_user` / `search_conversations` (PRD 04); `message.attribution` + `message.cost_usd`; the Projects + tags/archive specs below (project/tag/archived filters); Postgres FTS dialect guard (PRD 04). **Resolves §8 open question 8 ("Search scope")** toward FTS + structured filters, vector/semantic deferred to P2 with RAG. *Mobile-web + a11y:* filter chips wrap into a horizontally-scrollable row with a "Filters" disclosure on narrow viewports; each filter is a labeled control with active state announced; results reuse the accessible sidebar row pattern. *Phase:* **P1** (filters + Postgres FTS; vector/semantic deferred P2).
- **[P0]** Auto-title a new conversation from its first exchange; user can override via rename.
- **[P1 — pin shipped; archive + tags + bulk net-new]** Pin / archive / tags / bulk actions (conversation organization v2). Cite **D20**.
  - **Shipped:** pin/unpin a conversation — pinned threads surface in a dedicated "Pinned" group in the sidebar, persisted via `patchConversation({ pinned })`. **Archive, tags, and bulk actions are net-new** (only pin ships today).
  - **[P1] Net-new org layer.** *Wedge:* core chat loop (history scales past the flat recency list once the beachhead persona accumulates hundreds of chats) + privacy (bulk archive/delete/retention is a data-control multiplier). **Design decision (on-strategy):** rather than three overlapping hierarchies, we cut to **Projects (below) = the one single-level container**, **tags = orthogonal cross-cutting labels**, **archive = a lifecycle state**; nestable folders are explicitly deferred to **P2** to keep the mental model tight.
    - *Behavior:* **Tags/labels** — attach 0–N freeform (color-optional) tags to a conversation; filter history by tag (chip row / via the advanced-search filters above); tag rename/merge/delete in settings. **Archive** — a kebab + swipe + bulk action that moves a conversation out of the default history view into an "Archived" filter; archived chats are excluded from the default list **and from the assistant's retrieval-over-history** (below) but remain searchable and restorable (this adds the `archived` lifecycle the data model lacks today). **Bulk actions / multi-select** — a selection mode (checkbox-on-hover desktop, long-press mobile) enabling bulk archive / move-to-project / add-tag / delete; bulk delete confirms with a count. Pin stays orthogonal (a pinned chat can also be tagged/archived; unarchiving keeps the pin).
    - *AC:* adding a tag persists it and filtering by it shows exactly the tagged conversations; archiving removes a chat from the default list + retrieval-over-history candidate set while keeping it findable via the Archived filter and restorable; bulk-selecting N and choosing an action applies it to all N atomically (or reports partial failure clearly), bulk delete shows the count; deleting a tag removes it from all conversations without deleting them; all actions are keyboard-reachable (multi-select togglable via keyboard, §5.7) with selection state announced.
    - *Data/transparency/privacy/cost:* new `conversation.archived` (+ partial index for the default not-archived list), a `tag` table + `conversation_tag` join (avoids an array column so rename/merge is one update) — all owned by PRD 04 §6. No provider calls. Bulk archive/delete is a data-control accelerant; archive composes with retention (an archived chat still obeys its project/user retention TTL — not a retention bypass). Tags are user data → covered by export/erase (PRD 04 §5.7). *Dependencies:* `conversation` table (new `archived` col); new `tag` / `conversation_tag` tables; the sidebar grouping/selection model; the Projects spec (shared move-to-project bulk action) + advanced search (tag/archived filters) above; retrieval-over-history (exclude archived). *Mobile-web + a11y:* multi-select via long-press (selection mode disables swipe); tag chips are labeled ≥44px targets in management; the selection toolbar is a labeled region announcing the count ("3 selected"); reduced-motion respected on list enter/exit transitions. *Phase:* **P1** (archive is the named P1 item; tags + bulk are the coherent org layer; nested folders deferred P2).
- **[P1]** **Retrieval-over-history** — the assistant can **search past chats as a tool** it invokes mid-conversation ("let me check our earlier chats…"), distinct from the §4.5 UI search box (which is a user-driven find). This is a tool-call surface (renders as a tool-call part, §4.4) owned jointly with **PRD 02** (retrieval) and **PRD 04** (index). **Archived conversations are excluded from the retrieval candidate set** (per the org-v2 archive lifecycle above). Note: incumbents gate the higher-value retrieval-over-history behind paid plans even where memory is free — treat it as the gated, higher-value capability.
- **[P1] Projects/Spaces (thin scoping containers — advances FR-21's Projects to P1). Cite D20.** Replaces the former `[P2] Folders / tagging / Projects` placeholder. *Wedge:* cost (a project-scoped default tier + budget sub-cap concentrate cost control where work happens) + privacy (project-scoped retention — e.g. a "client work" project on 30-day retention while personal chats stay forever) + core chat loop (organizational continuity for multi-conversation efforts). **Framed deliberately away from breadth:** a Project is **not** a workspace productivity suite (a PRD 00 §1/§3 guardrail) — it is a thin container that scopes the *existing* wedge controls (model tier, retention, budget, custom instructions) to a set of conversations. Net-new (no `project` table today; conversations attach only to a user).
  - *Behavior:* **Create a Project** ("+ New Project" in the sidebar) with a name, optional color/emoji, and optional **project instructions** (a project-scoped custom-instructions block, same semantics as the global one). **Project-scoped defaults:** default model **tier** (overrides the user's global default for chats started in the project), optional **retention override**, and an optional **monthly budget sub-cap** (advisory/visible; hard enforcement stays the global cap's job — PRD 04). **Conversations live in a project** (or "No project"), show a project chip in the thread header, and have a **move-to-project** action (kebab + the bulk action above). **Project view** filters the sidebar/history to that project and surfaces its instructions + defaults. A Project **may reference a knowledge base**, but that retrieval engine is **PRD 02's RAG scope** — this spec only reserves the association, it does not build retrieval. **Instruction layering:** a chat in a project injects **global custom instructions + project instructions** (both as untrusted-preference blocks, project block clearly labeled), sequenced into the cache-stable prefix per **PRD 02 FR-37**; project instructions never override safety/system rules. Memory (§4.8) stays **account-global** at P1 (project-scoped memory is a P2 refinement, flagged below).
  - *AC:* creating a project persists a `project` row owned by the user; a chat created inside it has `conversation.project_id` set; a project default tier routes new chats in that project to that tier while the picker still allows per-turn override (no silent lock-in — §4.6 served-vs-requested); project instructions are injected (project-labeled) in addition to global ones without mutating the cache-stable prefix (FR-37); a project retention override purges that project's chats on its own TTL; deleting a project prompts for disposition — move its chats to "No project" (default) or delete them too (confirmed), never silently orphaning a conversation; moving a chat between projects re-scopes its defaults for *future* turns only (past attribution unchanged).
  - *Data/transparency/privacy/cost:* new `project` table + nullable `conversation.project_id` (NULL = "No project") owned by PRD 04 §6. **Transparency:** project defaults never silently downgrade — the served model is still shown per message; a project default tier is a *starting* selection, overridable, surfaced in the picker. **Privacy:** project-scoped retention is a privacy win (segment sensitive work onto short retention); it composes with the cross-store delete/retention cascade (PRD 04 §5.7). **Cost:** the project default tier + budget sub-cap concentrate cost control; enforcement stays the global budget gate (PRD 04), the sub-cap is a scoped advisory reading the same `message.cost_usd` ledger. *Dependencies:* `conversation` table (new `project_id` + migration); the instruction-injection seam (PRD 02 §4.5/§4.9); retention purge + budget gate (PRD 04 §5.6/§5.7); the sidebar/org surface above. *Mobile-web + a11y:* Projects render as a collapsible section above the recency groups in the sidebar; on mobile the project switcher is a labeled select / bottom sheet; the project header and instructions editor are labeled regions; the create-project dialog traps focus and restores it on close (§5.7); color/emoji is decorative (`aria-hidden`), never the sole identifier. *Phase:* **P1.** *Open refinement (P2):* project-scoped memory and nestable folders are deferred to avoid over-building.
  - *Cross-domain note:* a **Project** (conversation container) is distinct from a **Persona** (a reusable model+prompt+tool config, PRD 02, P2) and from a **knowledge base** (a retrieval corpus, PRD 02 RAG, P2): a Project MAY hold a KB and MAY later default to a Persona, but it owns neither. Shared/team Projects ride this same container (PRD 05 workspaces, P2).

### 4.6 Message actions
- **[P0]** **Copy** assistant message (icon row under message; also copies as clean markdown).
- **[P0]** **Regenerate** the most recent assistant response.
  - **[shipped] Regenerate-with-model:** the Regenerate control is a **split button** — the primary click re-runs with the currently-served tier, and a chevron menu picks **any other capability tier, or an alternate available provider route** for the served tier (`message-actions.tsx` `RegenerateMenu`; `chat-thread.tsx` `handleRegenerateWith`, which also moves the picker selection to match going forward). This generalizes and resolves PRD 07 §10.3's "retry with the requested model" open question.
- **[P0]** **Edit last user message + re-run** (in-place edit of the latest user turn, then regenerate). Scope edit to the most recent user message in MVP to avoid implicit branching/history-rewrite complexity. (Note: in 2026 ChatGPT *restricted editing/retry of arbitrary older messages* — a separate, likely model-version constraint; this is **not** a "branching retreat," and is **not** the rationale for edit-last. We constrain edit-last purely to keep the data model simple.)
- **[P0]** **Thumbs up / down** feedback; thumbs-down opens an optional detail note. (Storage/usage of feedback: PRD 04.)
- **[P0]** **Per-message model attribution**: each assistant message shows which model/tier produced it (transparency requirement).
- **[P0]** **Served-vs-requested transparency (silent-downgrade prevention) — this PRD owns the surface.** When the model/tier that actually *served* a message differs from what was *requested* (unavailable, rate-limited, or auto-routed to a cheaper model), the per-message attribution must **visibly indicate the substitution and a short reason** ("answered by Fast because Pro was rate-limited"). The routing/availability/decision logic is owned by **PRD 02** (reference it); the per-message indicator is owned here. This is the single highest-value transparency moment and it lives on the chat surface.
- **[P1] "Memory used here" per-message indicator (the memory analogue of served-vs-requested). Cite D19.** The per-message trust moment for the memory ledger (§4.8): when a turn was generated with ≥1 stored memory fact in context, the assistant message footer shows a subtle **"Memory used"** chip alongside the existing model-attribution + cost chips. *Wedge:* transparency + privacy — the user sees, per answer, exactly which stored fact was available to it.
  - *Behavior:* tapping/hovering the chip reveals a small popover listing the **specific facts** that were in context for that turn, each with a "learned from <conversation>" link; the user can **edit / delete** a fact directly from the popover (writes through to the §4.8 store) or **archive** it ("don't use this for answers"). If memory was *on* but no facts were injected (none relevant / under budget), **no chip appears** — never a misleading "memory used" when it wasn't (mirrors §4.2's "no empty reasoning panel" discipline). Copy is framed honestly as *what was available to the model* ("Memory in context" / "Memory used here"), not a fabricated claim the model leaned on each fact — the same epistemic honesty as the reasoning panel showing only emitted reasoning.
  - *AC:* a turn with injected memory renders the chip and the popover lists exactly the facts injected for that turn (from the persisted injection record, not recomputed); a turn with memory enabled but zero facts injected renders **no** chip; editing/deleting/archiving a fact from the popover persists and changes the next turn's injection; provenance links resolve to the source conversation, degrading gracefully for a deleted source ("learned from a deleted chat").
  - *Data/transparency/privacy/cost:* persist the injected-fact set per assistant turn — cheapest shape is an `injectedMemoryFactIds` array inside the existing `message.attribution` JSON (no new column), joined back to memory rows live for the popover. **Privacy — hard rule:** the chip and popover are **omitted from public share links** (memory facts are private user data; consistent with the model-attribution-yes / cost-no public-share rule — §4.10, and pinned in **PRD 07 §6.4**); they are retained in the in-app view and in private export. No cost beyond the §4.8 injection record. *Dependencies:* §4.8 memory store (the persisted injection record); `message.attribution` (PRD 04); the public-share serializer; PRD 07 §6.4 (memory share-exception). *Mobile-web + a11y:* the chip joins the responsive footer chip row; on mobile the popover opens as a bottom sheet (hover is unavailable on touch — §5.6 citation-popover precedent); chip has an accessible name ("Memory used — 2 facts in context"); each fact is a list item with labeled edit/delete controls. *Phase:* **P1** (ships with the §4.8 ledger).
- **[P1 — shipped]** **Explicit copy-on-branch ("branch from here" / "branch in new chat")** — **pulled to P1 (was P2).** On hover/overflow, copy the conversation up to a chosen point into a **new** thread, leaving the source intact. This is a low-risk *copy* model (not an in-thread tree) and directly serves the dev/power-user beachhead; it is now a shipped, standard power-user feature, not something incumbents retreated from. In-thread alternate-response trees remain **P2** (see §8).
  - **Shipped:** a "Branch in new chat" message action copies the thread up to that message into a fresh conversation, leaving the source intact (`message-actions.tsx`; `chat-thread.tsx` `handleBranchFromMessage`).
- **[P1] Read aloud (text-to-speech) for assistant messages — promoted from a stub to a full spec. Cite D22.** A **"Read aloud" action** in the assistant message footer action row (alongside copy / regenerate / thumbs) lets users consume long answers hands-free/eyes-free (mobile, multitasking, low-vision, dyslexia, reading-fatigue) — a high-leverage accessibility differentiator. The synthesis capability/registry/pricing is owned by **PRD 02 §4.8** (the STT/TTS spine — `modalities_out` + per-route audio pricing + `data_policy`); this PRD owns the in-thread control + behavior. No read-aloud/TTS code exists today — net-new.
  - *Wedge:* accessibility (primary) + mobile-web-first polish + core chat loop (consumption, not just production).
  - *Behavior:* clicking starts playback of that message; the control toggles to a Stop/pause state. **Auto-read toggle (opt-in, off by default)** in settings reads each completed assistant turn automatically (off by default to avoid surprise audio). **Works during and after streaming** — read-aloud can begin on settled sentences as they stream (the same "render once balanced" discipline §5.4 uses for math) and continue to completion, or be invoked on a finished message; a turn stopped mid-stream reads only the preserved partial (consistent with §4.1). **Two-track engine, capability-detected:** Track A = Web `speechSynthesis` (zero-infra, free, default, honors system voices); Track B = server/BYOK neural TTS (higher-quality voices) routed through the PRD 02 §4.8 provider abstraction, billed per character/second. **Voice + rate controls** (a small popover or settings): voice selection + a **0.5×–2× playback-rate**, persisted per user. **Reading-position highlight** (a11y) — the current sentence/word is highlighted caption-style while reading (honors `prefers-reduced-motion`: highlight without animated scroll). **Skip/markup discipline** — code blocks, tables, and raw markup are summarized or skipped ("code block, 12 lines") rather than read character-by-character; reasoning-panel text is **not** auto-read (collapsed by default).
  - *AC:* each assistant message exposes a labeled "Read aloud" control reflecting playing/paused state; with auto-read on a newly completed turn begins reading, with it off (default) no audio plays unless invoked; read-aloud can start on a still-streaming message and continues to the end, and a stopped/partial turn reads only its preserved text without erroring; voice + rate selections persist across sessions; code/table/markup is not read verbatim; all controls are keyboard-operable and screen-reader-labeled, and starting/stopping playback does not hijack the AT's own speech; reduced-motion users get reading-position indication without animated scroll.
  - *Data/transparency/privacy/cost:* Track A sends nothing to our servers; Track B posts message text to the TTS route — disclose the route's `data_residency` (PRD 02 §4.8) on first server-TTS use. **Transparency spine on output media:** when Track B (a billed neural voice) is used, the synthesized audio is attributable — *which voice/model produced it and what it cost* — surfaced in the message cost detail (`cost_breakdown`, PRD 07 §4); Track A (free OS voice) records zero cost with `cost_confidence` intact. Per-character/second TTS cost accrues to the usage meter (platform key) or BYOK key, never marked up for BYOK (PRD 07 §6.3). *Dependencies:* **PRD 02 §4.8** (TTS binding, voice registry, audio pricing, disclosure); dictation (§4.3, shares the two-track/disclosure pattern); the §5.7 accessibility baseline (a synchronized read-aloud caption/highlight is a P2 a11y extension that reuses this reading-position machinery, owned in PRD 03 §4.8). *Mobile-web + a11y:* this is a flagship a11y feature — acceptance-blocking quality, not a buried toggle. Mobile autoplay policies require a user gesture for first playback (auto-read's first turn may need a one-time "enable audio" tap); `Screen Wake Lock` (PRD 03 §4.9) should hold during long read-aloud and release on stop. *Phase:* **P1** (the Web-Speech Track-A path is near-zero infra).

### 4.7 Onboarding / empty state
- **[P0]** Centered friendly greeting + **3–4 suggested-prompt cards** showcasing variety of use cases + a focused composer.
  - *AC:* Clicking a card populates the composer (or sends immediately — see open question §8) and clears the empty state.
- **[P1]** Use-case template gallery.

### 4.8 Settings basics
- **[P0]** **Theme**: light / dark / **system** (default system).
- **[P0]** **Custom instructions** entry point (stable user preferences injected per chat; content semantics in PRD 02). MVP = a simple text-entry dialog reachable from settings and a shortcut.
- **[P0]** **Data controls** entry point: clear/delete history; training opt-out toggle if applicable (privacy-first posture). Detailed data handling in PRD 04. *(Shipped; retention options are concretely 30 days / 90 days / forever — `settings-dialog.tsx`.)*
- **[shipped]** **Telemetry opt-out:** a "Product telemetry" toggle (content-free usage events) sits alongside the training opt-in/out in settings (`settings-dialog.tsx`).
- **[shipped]** **Billing entry points:** Upgrade-to-Pro and Buy-credits checkout plus a Manage-billing portal link, **gated for non-anonymous users** (disabled / "Sign in to upgrade" while anonymous; `settings-dialog.tsx`). The monetization model itself is owned by **PRD 05**; this surface only hosts the entry points.
- **[P1] Usage & Insights — a longitudinal spend-analytics surface (the transparency wedge made cumulative). Cite D27.** A dedicated **Insights / Usage** surface (a settings tab on mobile-web; a panel/route on desktop) above the shipped current-month meter (`usage-meter.tsx` byline + `UsageDetails`/`BudgetEditor`). Today a user sees the cost of *this* message and a single current-month meter; they cannot answer "what did I spend this month vs last", "which model/conversation is burning my budget", "how much did BYOK save me", or "am I on track to hit my cap". This is the most defensible deepening of the per-message-cost wedge we already own (incumbents expose none of it) and feeds the activation funnel ("opened usage meter" is a first-week success signal — PRD 05 §6). The monetization model + metrics are owned by **PRD 05 §6**; the optional rollup table/aggregation endpoint by **PRD 04 §6**; this PRD owns the chat-shell surface.
  - *Wedge:* transparency (per-message cost → **longitudinal** spend you can see, slice, and trust) + cost leadership (makes the savings from cheap routing/BYOK *legible* — the proof of the wedge).
  - *Behavior:* **Time series** — spend (USD) + message count over selectable ranges (this period / last period / 30 / 90 days), defaulting to the current calendar-month to match the enforced cap grain. **Breakdowns** (sortable, each a share-of-total bar) — by **model/tier** (served-model label from attribution), by **conversation** (top-N spenders, deep-link to thread), by **day**, and a **platform vs BYOK** split (the "what BYOK saved you" line). **Budget burn-down** — for users with an enforced cap, a progress line projecting period-end spend vs the effective cap (reuse `effective_quota_usd = min(operator quota, user cap)`), plus credit-balance runway; this is the surface where the **budget alerts / soft-cap / per-conversation cap** (PRD 05 / PRD 08, D27) are set and visualized. **Cost-quality context** — each model row optionally shows avg cost/message + (where votes exist) the user's own thumbs-up rate, so "expensive" is seen against "good" (a soft bridge to the §4.3 value-aware picker). **Export** — CSV + JSON of the per-period / per-model / per-conversation rollup (extends the shipped GDPR export).
  - *AC:* a new read endpoint (e.g. `GET /api/usage/analytics?range=…&groupBy=day|model|conversation`) returns server-aggregated spend + counts from `message.cost_usd` + `attribution` + `created_at`, scoped to the caller's conversations only; aggregation never exposes message *content* — only cost, served-model label, conversation id/title, timestamps, counts; **sums reconcile and label their basis** — the dashboard MUST label whether each number uses the cumulative `usage_rollup` meter (every generation triggered) or `sum(message.cost_usd)` (surviving messages only) and never silently mix them (cross-ref PRD 07 cost basis); BYOK turns are shown but visually/labelled distinctly (no platform budget/credit consumed); burn-down uses the same `effective_quota_usd` rule as enforcement; export is rate-limited like the GDPR export and contains no secrets; empty/low-data states (new user, BYOK-only, telemetry-off) render gracefully; anonymous users get a scoped session view or a sign-in prompt (consistent with billing gating).
  - *Data/transparency/privacy/cost:* data is **already captured** (`message.cost_usd`, `message.attribution` incl. `isByok`, `message.created_at`, `conversation_id`) — **no schema change strictly required** for v1, but an optional denormalized `usage_daily_rollup(user_id, day, served_tier_id, provider_id, is_byok, cost_usd, message_count)` table is recommended for query performance at scale (PRD 04 owns the table + migration after head **0018**). **Privacy:** deals only in cost/metadata (honors the content/cost separation); deleted conversations drop out of aggregates (retention purge); does **not** require telemetry opt-in (it's the user's own money data), though the quality overlay is clearly the user's own votes. **Cost:** read-only; the rollup table (if built) adds a small write on terminal alongside the existing `usage_rollup` upsert. *Dependencies:* the shipped `usage_rollup` + `message.cost_usd` + attribution contract (PRD 07); `effective_quota_usd`; pairs with the §4.3 value-aware picker (quality/value data) and the budget alerts (PRD 05/PRD 08); **PRD 04 §6** (rollup table/endpoint); **PRD 05 §6** (metrics). *Mobile-web + a11y:* charts must have a **table/text equivalent rendered first** (WCAG 2.2; non-color encodings); respect `prefers-reduced-motion` (no animated draw-in); range/breakdown toggles ≥44px; aggregate **server-side**, never compute over thousands of messages on the main thread (INP budget, PRD 03). *Phase:* **P1** (a read/aggregation + UI layer over data already captured; PRD 05 §6 lists "Richer usage-credit UX" as P1 — this is its analytics sibling).
- **[P0]** **Temporary chat** entry point in the New Chat menu and thread header. Temporary chats are visibly marked, excluded from future memory/personalization, and use the retention fields defined in PRD 04. *AC:* starting a temporary chat sets `chat.is_temporary = true` and the UI shows a temporary-chat banner.
- **[P1] Memory transparency UX — the glass-box ledger (the differentiated spec; memory stays P1). Cite D19.** Memory is now a free baseline everywhere, so our wedge is the transparency layer, not the feature: (a) a **viewable/editable memory ledger** (see/add/edit/delete stored facts); (b) a per-message **"memory used here" indicator** (§4.6) showing which stored memory informed a response; (c) **cross-tool memory import** (from ChatGPT/Gemini/Grok) as a cheap acquisition lever (**P2** — lands after the core trust loop, deliberately decoupled from brittle third-party formats). Generic memory management without the ledger/indicator is off-thesis. Net-new: there is no `memory_fact` table, no extraction, no injection, and no memory UI today (only `preferences.custom_instructions`, injected per turn via `_apply_custom_instructions`).
  - *Wedge:* transparency (a per-fact-attributed ledger the user can read, correct, and prune — memory itself is commoditized, the glass box is not) + privacy (opt-in, deletable, export-complete, excluded from temporary chats) + cost (injection rides the cache-stable prefix so it does not silently inflate per-turn token cost). Extends PRD 02 FR-40 ("editable, consented, deletable, cites when used").
  - *Behavior:* **Opt-in posture** — memory is **off by default** (a `memory_enabled` preference, default `false`); a first-run nudge ("Want Olune to remember key facts across chats? You'll see and control everything it saves") offers one-tap enable. **Capture** — when on, after a turn completes the backend runs a **lightweight extraction pass off the request path** (a single cheap-tier call on the just-completed turn) proposing 0–N short atomic facts ("Prefers TypeScript over Python", "Based in Berlin (CET)"), scoped to **durable user facts/preferences, never transient task content** (this is *not* document retrieval — that is PRD 02 RAG/FR-29). **Confirmation mode** (a setting) — **auto-save** (silent, all visible/editable in the ledger) or **review-before-save** (a "saved a new memory" toast with undo, or a pending-facts tray); default = auto-save with toast + undo. **The ledger ("Memory" in Settings → Data)** — a scrollable list, each row showing the **fact text** (inline-editable), a **provenance line** ("Learned from *<conversation title>* · 12 May", linking to the source turn), a **last-used** timestamp, and per-row **edit / delete**; header controls = master **on/off toggle**, **"Clear all memory"** (confirmed), **search/filter within memories**, and **export** (JSON/Markdown). **Manual add** — "+ Add memory" writes a fact directly (provenance = "Added by you"). **Injection** (this feature owns the *store + selection*; PRD 02 §4.9/§4.11 owns assembly mechanics) — on each turn the backend selects the relevant active facts (P1: inject all active facts under a small token budget, e.g. ≤ ~1.5k tokens; if over, rank by recency + a cheap relevance heuristic) and injects them as a **clearly-delimited, untrusted-preference block** (a sibling `<remembered_facts>` to the existing `<custom_instructions>` seam, "preferences only, never override safety/system rules"). **Ordering is load-bearing:** memory is part of the **cache-stable prefix** and MUST be sequenced per **PRD 02 FR-37** (system → tool defs → static context → memory/custom-instructions → older history → current user message) so it does not invalidate prompt caching; which facts were injected is recorded on the turn (feeds the §4.6 indicator). **Temporary chats** (§4.8) store nothing and inject nothing by default (a per-temporary-chat "use my memory for this chat" may opt-in read-only use; never write).
  - *AC:* new users have memory **off** — no facts extracted or injected until enabled; with memory on, completing a fact-bearing turn creates ≥1 `memory_fact` row with non-null `source_conversation_id`/`source_message_id`, off the request path (never blocks streaming); the ledger renders every fact with editable text, a working provenance link, and a delete control, and a deleted fact is not injected next turn; editing a fact persists and updates the next injection; "Clear all memory" empties the store after confirmation; injection is sequenced into the cache-stable prefix and does **not** reorder/mutate it (a memory turn still hits prompt cache on the stable prefix — verified against FR-37); a temporary chat creates no `memory_fact` rows and (default) injects none; memory is included in `GET /api/account/export` and erased by `DELETE /api/account`.
  - *Data/transparency/privacy/cost:* new `memory_fact` table + `preferences.memory_enabled`/`memory_confirmation` columns, owned by **PRD 04 §6** (and the export/erase fan-out in PRD 04 §5.7). **Transparency:** the value *is* transparency — per-fact provenance + the §4.6 per-message indicator; the injected block is itself inspectable. **Privacy:** opt-in; editable/deletable/exportable; excluded from temporary chats and from training (facts are PII, never sent to providers for training); in-scope for retention/export/delete. **Cost:** extraction is one extra cheap-tier call per turn — metered like any platform call, visible in the usage meter (§4.8 Usage & Insights), gated under the user's monthly budget cap (skip extraction when the gate would block); injection rides the cache-stable prefix so its marginal cost is the discounted-input rate. *Dependencies:* PRD 02 §4.9 context assembly + FR-37 ordering (the injection seam); the existing `_apply_custom_instructions` seam; `preferences` (new columns); account export/delete (PRD 04 §5.7); the §4.6 "memory used here" indicator (its consumer). **Memory ≠ RAG:** memory = lightweight user facts/prefs injected as preferences; RAG (PRD 02) = document retrieval with citations — distinct stores, distinct UX. *Mobile-web + a11y:* the ledger stacks as full-width cards with the provenance line wrapping below the fact; edit/delete are ≥44px targets (PRD 03); each row is a labeled list item, edit enters an inline field with an accessible name ("Edit memory"); the master toggle and "Clear all" are labeled controls; the save toast uses the §5.7 polite status region (announced once); reduced-motion honored. *Phase:* **P1** (the extraction call + ledger UI are the build cost; depends on no unbuilt infra).

### 4.9 Keyboard shortcuts & command palette
- **[P0]** **`Cmd/Ctrl+K` command palette**: search/jump-to-chat, new chat, focus composer, toggle theme, toggle sidebar, open shortcuts list.
- **[P0]** Core shortcut set (see §5.5 table) including new chat, focus composer, copy last response, toggle sidebar, and a **show-all-shortcuts** dialog (`Cmd/Ctrl+/`). The default set **must be validated against browser/OS-reserved combos** (e.g., `Cmd/Ctrl+Shift+S` collides with "Save As"/screenshot in some browsers) before locking — do not blindly mirror one vendor's set.
- **[P1] User-customizable keyboard shortcuts (the net-new layer over the shipped fixed set). Cite D23.** Let power users remap shortcuts — incumbents lock theirs, so this is a cheap differentiator for our persona, and an **accessibility** win (users with motor/keyboard constraints can choose reachable chords). The shipped dispatcher (`use-keyboard-shortcuts.ts`) is a clean capture-phase, IME-safe, Mac/Win-aware layer, but the descriptor array and the §5.5 default set are **hardcoded** — this makes that list user-overridable (not a dispatch rewrite). PRD 00 §5 explicitly defers *customizable* shortcuts to P1.
  - *Behavior:* a **Shortcuts** settings panel lists every shipped action (the §5.5 table) with its current binding rendered via the existing `KeyCaps`/`shortcut-format.ts`; each row has an "Edit" **recorder** that captures the next chord, validates, and saves. **Reserved-combo guard (load-bearing):** the recorder rejects/warns on browser/OS-reserved chords (`Cmd/Ctrl+W/T/N/Q/L`, `Cmd+Shift+S`-class "Save As", etc.) and on duplicate bindings (two actions → same chord), surfacing an inline error rather than silently shadowing. **Reset:** per-row "reset to default" + a global "reset all". The `Cmd/Ctrl+/` shortcuts dialog reflects the user's **effective** bindings (custom overlaid on defaults) — a single source of truth for both the dialog and the global dispatcher.
  - *AC:* a user can rebind any shipped action to an allowed chord that persists across reload and devices and takes effect without a redeploy; the recorder refuses a known browser-reserved combo and any chord already bound elsewhere, with an inline reason; "reset all" restores the exact §5.5 default set; bare-key shortcuts still never fire while typing in the composer (the shipped `allowInInput` rule holds for custom bindings too); the effective set is what both the dispatcher and the `Cmd/Ctrl+/` dialog use.
  - *Data/transparency/privacy/cost:* a small `shortcut_overrides` JSONB blob on the existing `preferences` table (`{actionId: chord}` overlaying defaults — **no new table**), owned by PRD 04; included in export/delete as user prefs; no provider cost, no transparency implications. *Dependencies:* the shipped `use-keyboard-shortcuts.ts`/`KeyCaps`/`shortcut-format.ts` + the shortcuts dialog; `preferences` persistence; should land *with* the prompt library (§4.3 C1) so both share a Settings home. *Mobile-web + a11y:* shortcuts are desktop-keyboard-centric — the panel is informational/secondary on mobile but stays reachable and screen-reader navigable; the recorder **announces captured chords** (§5.7); the dialog keeps its shipped focus-trap/restore. *Phase:* **P1** (shares the roadmap row with the prompt library).
- *AC:* All P0 actions reachable by keyboard alone; palette is fully keyboard-navigable and screen-reader friendly.

### 4.10 Sharing & export
- **[P0]** **Shareable unlisted conversation link** (read-only public-by-link; not indexed). Creating a link is explicit; user can revoke it.
  - *AC:* Shared view renders the conversation read-only with model attribution; no composer; revoke invalidates the link.
  - **Decision — public shares show model attribution but NOT per-message cost.** PRD 00's transparency contract is "model **and cost**"; we deliberately drop **cost** from *public* shares (privacy/competitive reasons) while keeping model attribution. Stated here so the share view doesn't silently diverge from the cross-cutting contract.
- **[P0 — shipped, text-only]** **Copy-as-markdown**: copy the whole conversation (or a single message) as clean markdown, plus **download-as-`.md`** for the whole conversation or a single message (`chat-thread.tsx` `renderConversationMarkdown` / `downloadMarkdown`). **Correction:** the exported payload is **turns + text only**; **per-message model / token / cost metadata and reasoning are intentionally OMITTED** from the markdown (reasoning dumps noisily; cost/token metadata stays on the in-app message rows and is not redacted there). This is distinct from the public-share view above, which omits cost from a *rendered* view. The export skips pending/errored turns so it represents the settled thread. (When memory ships, the export should *include* memory — beating ChatGPT's export, which historically excludes it.)
- **[P1]** Multi-format export (PDF / .docx / **Markdown file download — shipped**, see §4.10 P0 above). PDF and .docx remain deferred.
- **[P2]** Published "pages" (Perplexity-style formatted public artifact).

### 4.11 Citations / source cards + suggested follow-ups
- **[P1 — shipped (UI present; search backend default-off)]** **Citations are first-class message parts** (`citation` part type, §4.4), each carrying a **stable ID** that binds an inline marker to a structured source object — *not* a loose `[n]` token inside a markdown string. When web search/RAG is used (PRD 02 owns retrieval), render **inline numbered citations** `[1][2]` that, on click/hover, reveal source title, domain/favicon, and snippet; render a **source-card list** (right-rail on desktop, stacked below answer on mobile).
  - **Shipped:** a collapsible **source-card list** (`sources-panel.tsx`) — each card shows favicon, title, domain, and snippet, with the URL **scheme-sanitized** (only `http(s)` becomes a live link; anything else renders as inert text), so it is safe on the unauthenticated public-share view. The search backend that populates it is **`SEARCH_BACKEND` (default `none`)**, so cards only appear once a search backend is configured. **NOT yet shipped:** inline `[n]` markers, the pending→resolved streaming rule, and suggested follow-up chips.
  - **[P1] Inline `[n]` citation markers — build-ready (the near-term retrieval increment). Cite D24.** Upgrade from the light-spec above to a buildable feature: this is the highest-leverage, lowest-cost transparency increment in retrieval, and it closes the gap PRD 00 §5 names ("source-card list only, no inline `[n]` markers yet"). The backend **already** resolves ordered sources (`search/protocol.py::SourceItem` carries a 1-based ordinal `id`; Tavily, OpenAI multi-round, and Anthropic hosted-search all normalize to it) **and already instructs the model to cite `[1][2]`** — today those numbers render as **dead literal text** floating above an unconnected card list. The gap is **render-only**; making the markers live converts "a list of links" into "an answer you can audit sentence-by-sentence" (the Perplexity-grade citation UX our positioning claims). *Wedge:* transparency (a citation is transparency made tappable) + core chat loop (grounded answers are only trustworthy if you can jump to the source). The citation/source *contract* (grounded-vs-ungrounded honesty, provenance label, share rules) is owned by **PRD 07 §4.3** (D2); this PRD owns the chip/popover render.
    - *Behavior:* the renderer recognizes a citation token in answer text — bracketed positive integers `[1]`, `[2]`, and runs like `[1][2]` or `[1, 2]` → individual chips — and replaces it with a small **superscript chip**. A chip whose integer matches a `SourcesPart` item `id` is **resolved** (interactive); a chip with no matching source **degrades to plain text** (no dangling/dead `[n]`). **Resolved chip interactions:** click/tap → smooth-scroll to + briefly highlight the matching source card (auto-expanding the collapsed `SourcesPanel` first); desktop hover → popover with favicon, title, domain, snippet, whose primary action opens the URL in a new tab (the same `http(s)`-only scheme guard as the card). **Pending→resolved streaming rule** (load-bearing): because `[n]` can stream in the answer *before* the `sources` event arrives, a syntactically-valid-but-unresolved marker renders in a muted **pending** state and upgrades to interactive when its `SourceItem` lands (mirrors the §5.4 "render once balanced" math rule); after the turn settles, any still-unresolved marker degrades to plain text. **Disambiguation:** only parse markers when a `sources` part is present on the message, and treat `[1]` inside fenced/inline code (e.g. `arr[1]`) as literal — never a citation chip. Works identically for Tavily-backed OpenAI-compatible, Anthropic hosted-search, and the fake provider (all normalize to `SourceItem`).
    - *AC:* a web-search answer containing `[1]` renders it as an interactive superscript chip (not literal text) when source #1 exists in the turn's `SourcesPart`; clicking/tapping chip `[n]` scrolls to and highlights source card `n`, expanding the panel if collapsed; desktop hover shows a title+domain+snippet popover whose link target is `http(s)`-only; a marker that streams before its source shows a muted pending state then becomes interactive when the matching `SourceItem` arrives in the same turn; a marker with no matching id (e.g. `[9]` when 3 sources resolved) renders as plain text after the turn settles (never a dead link or console error); literal brackets in code on a non-search turn are **not** mis-rendered as chips; markers round-trip on reload + replay (derived from persisted `text` + `SourcesPart`); the public-share view renders the same interactive chips (sources are already in `PublicMessage`) with cost still stripped (PRD 07 §6.4).
    - *Data/transparency/privacy/cost:* **no new persisted fields** — chips are a pure function of the already-persisted `text` part + `SourcesPart`; source cards gain a stable anchor id keyed on `SourceItem.id`. *Optional hardening (spec now, defer building):* a backend "citation normalization" pass on turn finalization that validates emitted `[n]` against resolved ids and can rewrite/drop orphans, keeping the contract truthful when a model over-cites. No new privacy surface (sources are already shown); RAG/private-knowledge citations get the tighter share rule under PRD 07 §4.3 (private-document title/snippet redacted on public share). *Dependencies:* the shipped web-search path (`SEARCH_BACKEND`, `SourcesPart`, the `sources` SSE event — already in `main`); the §4.4 typed-parts / Streamdown renderer (markers are a streaming-safe inline transform that does not fight `parseIncompleteMarkdown`); **PRD 02 §4.7** (source/citation metadata shape); **PRD 07 §4.3** (the citation contract — D2). *Mobile-web + a11y:* the chip hit target clears the iOS 44pt floor (reuse the panel's grow-hit-area trick); on mobile the chip uses **tap-to-popover** (no hover), dismissible by tap-away/Esc; chips are real `<button>`s with an `aria-label` ("Source 1: {title}, {domain}"), keyboard-focusable in reading order, and activating one moves focus to / announces the target card; live-region announcements must **not** spam on each pending→resolved upgrade (debounce; announce the settled state); reduced-motion honored on the scroll/highlight. *Phase:* **P1 (near-term)** — the smallest, highest-trust increment; pull to the front of the retrieval roadmap. Spec'd in detail in §5.6.
- **[P1]** **Streaming resolution rule:** an inline citation marker may stream **before** its source metadata resolves. Render the marker in a **pending** state and resolve it (title/domain/snippet) when the source object arrives — mirroring the §5.4 "render once balanced" rule for math. A marker whose source never resolves degrades gracefully (no dangling `[n]`).
- **[P1]** **Suggested follow-up** chips under cited answers; tapping one sends it as the next message.
- *Note:* In-chat rendering of citations/cards is owned here; retrieval, citation metadata shape, and freshness are owned by **PRD 02**. Spec'd lightly in §5.6.

### 4.12 Artifacts / Canvas side panel
- **[P1 stretch]** **MVP-lite read-only side panel:** an **"open in side panel"** affordance on long code/document outputs — opens a read-only side panel (desktop) / full-screen toggle (mobile-web) with **copy** and **download**. No live execution, no versioning. (Stretch for the MVP window; drops to P2 if capacity is constrained — see §8.)
- **[P2 — full] Full Artifacts/Canvas: the versioned/editable successor to the P1-lite panel. Cite D23.** Where the P1-lite panel above is read-only (open-in-panel + copy + download), the P2 layer is the **editable, versioned** canvas — kept strictly **chat-anchored** (canvas content is always tied to a message part and a conversation; it is *not* a free-floating document workspace, which would cross the PRD 00 §1/§3 "chat product, not productivity suite" guardrail). Builds directly on the shipped **typed multi-part message model** (PRD 00 §11 D7). Net-new (no `artifact` table/route exists).
  - *Wedge:* core chat loop — a rendering/edit surface for chat output, not a standalone doc product; iterating on a substantial code/doc output ("change this function", "tighten paragraph 2") without full regenerations.
  - *Behavior:* **Promotion** — a long code/doc output gets an "Open in canvas" affordance (extends the §4.12-lite "open in side panel"), opening a desktop right-pane / mobile full-screen toggle. **Editing & versions** — the user edits content directly; each model revision and each saved user edit is a **version** in a linear history with a version switcher and a diff view (prev ↔ current). "Ask the model to revise" sends the current canvas content + the user's instruction as context and produces a **new version** (not an inline transcript reply), so the canvas stays the working surface. **Round-trip to chat** — explicit "send canvas to chat" / copy / download; editing the canvas never silently mutates the original message part — the original model output is preserved as **version 1** (provenance), edits layer on top. **Scope guardrail** — one canvas is bound to one conversation + originating message lineage: **no cross-conversation document library, no multi-user co-edit, no publish/embed at this phase** (the `[P2]` Published "pages" line above is the most product-y piece — deferred hardest). Code *execution inside* the canvas is a separate tool (a "Run" button on a code artifact invokes the sandboxed-code-execution tool owned by **PRD 02 §4.6**, P2) — they compose but ship independently.
  - *AC:* a code/doc output can be opened in an editable canvas with the original model output retained as version 1; user edits and model revisions each create a new version, switchable with a diff between any version and its predecessor; "revise with model" produces a new version using the current canvas content as input and the resulting turn carries normal model/cost attribution (transparency unbroken); closing/reopening the conversation restores the canvas + full version history (server-persisted); on mobile-web the canvas is a full-screen toggle that does not break the streaming transcript underneath, and copy/download work; canvas content renders through the same hardened streaming-safe renderer/sanitizer as inline content (§5.4) — no new XSS surface.
  - *Data/transparency/privacy/cost:* builds on the typed `Message.parts` model — introduce an `artifact` row (or part subtype) `{id, conversation_id, origin_message_id, kind, current_version}` + `artifact_version {artifact_id, n, content, author (model|user), created_at}`, owned by **PRD 04 §6** (reuse the `JSONB`/SQLite-variant pattern). **Transparency:** a "revise with model" turn is a normal billable turn with full attribution; pure user edits are free and clearly attributed to the user in version history; cost is incurred only on model revisions, surfaced on the message meter as usual. **Privacy:** canvas content is user/conversation content — export/delete coverage; included in conversation export. *Dependencies:* the typed multi-part message model (D7, shipped); the §4.12-lite read-only panel (this is its versioned/editable successor and on-ramp); the hardened renderer (§5.4, shipped via Streamdown); PRD 03 mobile fallback; PRD 02 §4.6 (the optional canvas "Run" composes with sandboxed code execution). *Mobile-web + a11y:* full-screen toggle on mobile (no cramped side-pane); the version switcher and diff are keyboard-navigable with labeled controls; the editing surface is a proper textarea/editor with an accessible name that respects the visual-viewport keyboard inset; reduced-motion honored on panel open. *Phase:* **P2** (full artifacts; the §4.12 read-only-lite stays P1-stretch and is its on-ramp).

---

## 5. UX & interaction details (load-bearing specifics)

### 5.1 Streaming behavior
- **State machine per assistant message:** `idle → submitted (pre-first-token indicator) → streaming (content + optional reasoning + status lines) → done | stopped | error`.
- **Pre-first-token:** show typing/skeleton; if no token within a soft timeout (e.g., ~10s), keep indicator but expose Stop prominently (latency budgeting in PRD 04/02).
- **Stop:** single click aborts; partial content frozen and persisted; UI transitions to `stopped` with a small "Stopped" tag; **Regenerate** offered. Stop must remain visible/sticky on mobile (PRD 03).
- **Auto-scroll:** follow the stream to the bottom **only if** the user is already at/near the bottom; if the user scrolls up to read, stop auto-following and show a "↓ Jump to latest" pill.
- **Error:** failed/aborted-by-server streams show an inline error with **Retry**, preserving any partial content.

### 5.2 Reasoning panel states
| State | Trigger | Visual |
|---|---|---|
| Hidden | No reasoning emitted | No panel, no affordance |
| Thinking (open) | Reasoning content streaming | Auto-expanded, shimmer + "Thinking…", live text |
| Done (collapsed) | Reasoning complete + answer streaming/done | Collapsed bar: "Thought for Xs", chevron to expand |
| Expanded | User expands | Full reasoning text (summary if provider only gives summary) |

- Reasoning text uses the same streaming-safe renderer (it may contain markdown/code).
- The panel is a **sibling above the answer**, clearly visually subordinate (muted), so it never competes with the answer.
- Never block the answer on reasoning: answer tokens may stream while/after reasoning.

### 5.3 Composer keybindings & layout
| Key | Action |
|---|---|
| `Enter` | Send |
| `Shift+Enter` | Newline |
| `Esc` (while streaming) | Stop generation |
| `Shift+Esc` (global) | Focus composer |
| `/` at line start | Open slash-command popover (shipped; client-static) |

> **`Esc` precedence:** When the composer is focused and a stream is active, `Esc` stops generation (it does **not** blur the composer); a second `Esc` is a no-op. When no stream is active, `Esc` follows default browser/textarea behavior.
>
> **IME caveat (a11y/i18n):** if an IME composition is active (`event.isComposing` true, or `keyCode === 229`), `Esc` must be left to the IME (CJK input uses Esc to cancel a candidate) and must **not** stop the stream. Validate this with a usability/screen-reader check given the mobile-web + EU/multilingual persona.

- Layout (desktop, P0): `[ multiline textarea ] [ model tier control ] [ Send/Stop ]`. **No attach control at P0**; P1 layout adds `[+ attach]` and attachment chips above the input when vision/PDF ships. Model control is compact (label = current tier) and opens a tiered menu with brief metadata (relative speed/cost, modality) sourced from the registry.
- Composer is sticky to the bottom of the thread; remains anchored during streaming.
- Mobile composer specifics (touch targets, keyboard avoidance, sticky Stop): **defer to PRD 03**.

### 5.4 Renderer requirements (spec carefully)
- **Streaming-safe parsing:** the renderer must tolerate incomplete markdown each frame: auto-close open code fences for rendering, defer table rendering until a row boundary is parseable, never render raw `*`/`#`/backticks as a flash. No layout shift > the height of the newly added content. *(Implemented via **Streamdown** — `parseIncompleteMarkdown` plus its allowlist hardening — in `markdown-renderer.tsx`.)*
- **Code blocks:** language label from the fence info-string; highlight progressively; copy copies raw source (not highlighted DOM); long blocks collapse with "show more"; (P1) download with sensible filename/extension from language.
- **Math:** KaTeX for `$…$` / `$$…$$`; render only once delimiters are balanced to avoid mid-token flashes.
- **Tables:** wrap in a horizontal-scroll container; sticky header optional; never overflow the viewport on mobile.
- **Images:** lazy-load, constrained max-width, alt text required (use model-provided alt or a generic label).
- **Links:** open in new tab, `rel="noopener noreferrer"`; show domain on hover.
- **Sanitization:** strip scripts/iframes/event handlers; allowlist-based HTML; this is both a quality and security requirement. *Mechanism:* **`rehype-harden`-class allowlist hardening** (as shipped in Streamdown) is the satisfying implementation for the §4.4 [P0 security] requirement; adopt Streamdown's hardening rather than a hand-rolled sanitizer.
- **Performance target:** rendering keeps up with stream at 60fps with no layout shift on a mid-tier mobile device; virtualize very long threads.
  - *Mechanism (implementation note — the lever behind the 60fps/no-shift target):* **buffer streamed deltas in a mutable ref and flush once per `requestAnimationFrame`** (rAF token-batching) rather than `setState` per token (the naive per-token re-render is the known jank anti-pattern); use **`scheduler.yield()`** to keep the main thread responsive and protect INP (optionally `startTransition`); parse markdown **incrementally per chunk (O(n)), not a full re-parse (O(n²)) every frame**, paired with the renderer's memoization model. This is the same pattern mirrored in PRD 03 for mobile.

### 5.5 Keyboard shortcuts (global)
| Action | Mac / Win-Linux |
|---|---|
| Command palette / search | `Cmd+K` / `Ctrl+K` |
| New chat | `Cmd+Shift+O` / `Ctrl+Shift+O` |
| Focus composer | `Shift+Esc` |
| Stop generation | `Esc` (while streaming) |
| Copy last response | `Cmd+Shift+C` / `Ctrl+Shift+C` |
| Copy last code block | `Cmd+Shift+;` / `Ctrl+Shift+;` |
| Toggle sidebar | `Cmd+Shift+S` / `Ctrl+Shift+S` |
| Open custom instructions | `Cmd+Shift+I` / `Ctrl+Shift+I` |
| Delete current chat | `Cmd+Shift+Backspace` / `Ctrl+Shift+Backspace` |
| Show all shortcuts | `Cmd+/` / `Ctrl+/` |

Conventions follow established category norms (low learning cost). A visible, searchable shortcut dialog is required for discoverability. This table is the **default** set; per §4.9 (customizable shortcuts, D23) it is overlaid by user overrides (`preferences.shortcut_overrides`), and the `Cmd/Ctrl+/` dialog and the global dispatcher both render the resulting **effective** set (single source of truth).

### 5.6 Citations & follow-ups rendering (build-ready; P1 — see §4.11, D24)
- Inline marker `[n]` is a small superscript chip **keyed on the matching `SourcesPart` item `id`** (§4.11). The renderer parses bracketed positive integers (`[1]`, `[2]`, runs like `[1][2]` / `[1, 2]` → individual chips) **only when a `sources` part is present on the message**, and treats brackets inside fenced/inline code (e.g. `arr[1]`) as literal. A chip matching a resolved source is interactive; click/tap smooth-scrolls to + briefly highlights the matching source card (auto-expanding the `SourcesPanel` if collapsed); desktop hover shows a popover with favicon + title + domain + snippet (link target `http(s)`-only, matching the card).
- **Pending→resolved:** a marker streamed before its source resolves renders in a muted **pending** state and upgrades to interactive when the `SourceItem` arrives (mirrors the §5.4 "render once balanced" rule); after the turn settles, any still-unresolved marker (e.g. `[9]` with 3 sources) degrades to plain text — no dangling/dead `[n]`. Markers round-trip on reload/replay (derived from persisted `text` + `SourcesPart`, no new persistence) and render identically on the public-share view (sources retained, cost stripped — PRD 07 §6.4).
- Source cards: favicon, title, domain, short snippet, opens in new tab; each carries a stable anchor id keyed on `SourceItem.id`. Right-rail on wide screens; collapsible list under the answer on mobile.
- Follow-up chips: max ~3–4, tappable to send.
- *a11y:* chips are real `<button>`s with an `aria-label` ("Source 1: {title}, {domain}"), keyboard-focusable in reading order, ≥44pt hit target; on mobile the chip is tap-to-popover (no hover), dismissible by tap-away/Esc; live-region announcements debounce pending→resolved upgrades (announce the settled state); reduced-motion honored on scroll/highlight.

### 5.7 Accessibility baseline (deliberate differentiation lever)
- **Labeled controls:** every icon button has a descriptive accessible name (Copy, Regenerate, Edit, Thumbs up/down, Attach, Send, Stop) — explicitly fixing the unlabeled-button gap measured in incumbents.
- **ARIA live regions (announce model — get this right; it is the a11y wedge):** the **streamed answer text node must NOT be wrapped in an `aria-live` region** — doing so makes NVDA/JAWS re-read the partial text token-by-token (a known anti-pattern). Instead, announce **discrete status transitions** ("Generating", "Response ready", "Stopped") via a **separate polite status region** (`aria-live="polite"`, distinct from the message body). The completed message body is **navigable but not auto-announced** (avoiding the inverse Claude bug where nothing is announced at all). The **success-path completion announcement** ("Response ready") is specified in PRD 08 §9 — this surface implements it. Status lines render their progress through the same status region.
- **Full keyboard operability:** every action (send, stop, copy, regenerate, edit, navigate history, open palette, expand reasoning) reachable and operable by keyboard with visible focus states.
- **Semantic structure:** messages as a navigable list with roles; reasoning panel exposes expanded/collapsed state via ARIA.
- **Keyboard-shortcuts dialog accessibility (beats a measured leader gap):** the show-all-shortcuts dialog (§4.9, `Cmd/Ctrl+/`) **traps focus on open, receives focus**, is screen-reader navigable, and returns focus to the invoking control on close — directly fixing the inaccessible-shortcuts-dialog blocker documented in incumbents.
  - *AC:* opening the shortcuts dialog moves focus into it; Tab cycles within it; Esc closes and restores focus; every shortcut row is reachable and announced by a screen reader.
- **Sidebar/history landmarks (beats a measured leader gap):** the history sidebar (§4.5) exposes **landmark roles** (e.g., a labeled `navigation`/`complementary` region) so screen-reader users can jump to it directly instead of traversing from top of page.
  - *AC:* the sidebar is reachable as a named landmark; history items are a navigable list with accessible names.
- **Reduced motion:** honor `prefers-reduced-motion` (shimmer/typing animations degrade gracefully).
- **Contrast & target size:** meet WCAG AA contrast; touch targets sized per PRD 03.
- Treat a11y as acceptance-blocking for MVP, not a follow-up.

---

## 6. Dependencies & cross-references

| Concern | Owner PRD | What this PRD relies on |
|---|---|---|
| Model registry, capability tiers, auto-routing, per-message model metadata, cost/token accounting (incl. reasoning tokens) | **02 AI Capabilities** | Tier names + metadata for the picker; which model answered each message; reasoning/summary payload shape; attachment handling semantics; web-search/citation metadata; TTS for read-aloud |
| Reasoning/"thinking" payload shape (summary vs raw; provider differences; effort knobs) | **02 AI Capabilities §3** | What the reasoning panel renders and the effort toggle maps to |
| Mobile composer, keyboard avoidance, gestures, touch targets, sticky Stop, mobile artifact fallback (full-screen toggle) | **03 Mobile / Responsive** | All mobile-specific composer/keyboard/layout details; this PRD only sets responsive intent |
| Streaming transport (SSE/resumable), abort propagation, persistence, search index, share-link backend, feedback storage, data controls/export plumbing | **04 Architecture** | Stream lifecycle, cancellation, conversation storage, full-text search, unlisted-link generation/revocation |
| Output sanitization / untrusted-content posture | **02 §9 + 04** | Renderer treats model/web content as untrusted |

**Deferral rule:** any mobile composer/keyboard specifics raised here are intentionally light — **defer to PRD 03** for the authoritative spec.

---

## 7. Success metrics (this surface)

| Metric | Definition | Target (MVP) |
|---|---|---|
| **Perceived time-to-first-token (TTFT)** | Send → first rendered content token | p50 < 1.0s, p95 < 2.5s (front-end portion; provider latency per PRD 02) |
| **Stream smoothness** | Dropped frames / jank during stream on mid-tier mobile | 0 layout-break events; sustained 60fps render |
| **Render correctness** | % of responses with code/math/tables that render without breakage (sampled + golden-set tests) | > 99% |
| **Stop-preserves-output** | % of Stop actions that retain partial content | 100% |
| **Regeneration rate** | % of assistant messages regenerated | Track as a quality signal (high rate may indicate answer or UX issues) |
| **Thumbs-down rate** | Negative feedback per assistant message | Track; trend down over time |
| **Copy / share usage** | Copy-as-markdown + share-link events per active conversation | Track (engagement/value proxy) |
| **Accessibility** | Automated a11y (axe-class) pass on chat surface + manual screen-reader smoke | 0 critical violations; labeled-control coverage 100% |
| **Keyboard reachability** | % of P0 actions doable without mouse | 100% |
| **Empty-state activation** | % of new users who send a first message (incl. via suggested cards) | Track as activation KPI |

---

## 8. Open questions / risks

Pulled from research open-questions and shared context:

1. **Branching data-model caution (risk) — narrative corrected.** Earlier drafts justified deferring branching by citing an "industry retreat from branching." That was a factual error: ChatGPT *launched* explicit **"Branch in new chat"** (Sep 2025, still live), and branching is standard in LibreChat/LobeChat; the 2026 ChatGPT change only restricted **editing/retry of arbitrary older messages** (a separate, likely model-version constraint). The correct lesson: **explicit copy-on-branch is good and shipped** (now **P1**, §4.6); only **implicit edit-an-old-message-rewrites-history** is the fragile pattern we avoid. **Decision for MVP:** edit is restricted to the most recent user turn (re-run in place, no tree) purely to keep the data model simple — *not* because of any "retreat." In-thread alternate-response **trees** remain **P2**; study a concrete fork implementation before designing that data model.
2. **Mobile artifact fallback (open).** Side-by-side panels are awkward on mobile-web; exact full-screen-toggle behavior must be defined with **PRD 03** before building even the MVP-lite side panel.
3. **Suggested-prompt card behavior (open).** Do cards send immediately or just prefill the composer? Default proposal: **prefill** (lower commitment, lets users edit), pending quick usability check.
4. **Slash commands — RESOLVED (committed P1).** Previously framed as a "risky/unproven" scope question; now a **committed P1 differentiator** (native `/` prompt library, §4.3) reframed as an underserved power-user need our persona already hacks around via extensions. No longer open.
5. **Token/char hints (open).** Consumer products generally hide these. Default: **omit** in MVP; reconsider only with evidence.
6. **Reasoning content storage (privacy/risk).** Providers may only return a *summary*, and some omit thinking by default (PRD 02 §3). We must **not** store or display reasoning the provider hides; the panel renders only what's emitted.
7. **Citation interaction details (partly resolved / open).** The data binding is now decided: citations are first-class parts with stable IDs and a pending→resolved streaming rule (§4.11). Still open: hover vs click and right-rail vs inline expansion — finalize with retrieval design (PRD 02) when web search lands.
8. **Search scope — RESOLVED for the advanced layer (P1).** Title-only vs full-text history search at MVP depends on the search index (PRD 04); target full-text if feasible, fall back to title + recent-content otherwise. The advanced history-search spec (§4.5, D21) resolves the *direction*: **Postgres full-text search + transparency-native structured filters** (served model/tier, cost, date, tag, project), dialect-guarded to fall back to LIKE on SQLite; **true vector/semantic search is explicitly deferred to P2** (it needs pgvector and lands with RAG/PRD 02 retrieval). The P0 baseline above (title + recent-content substring) still holds as the floor.
9. **Sourcing caveat.** Research is partly secondary; re-verify any specific shortcut/limit against first-party docs before locking. Shortcut conventions here follow category norms and are safe; exact provider limits are not owned by this PRD.
10. **Contested cross-PRD items this surface touches (FLAGGED, not decided here).** Per the product owner, these are not unilaterally decided in this PRD; integrated as open flags only:
    - **Exact Pro price.** This PRD shows a **"Pro" capability-tier label** in the composer/model picker (§4.3, §5.3) and can include per-message cost metadata in copy/export (§4.10); none of those depend on the Pro *price*, which is owned by **PRD 05**. Metered Pro + P0 USD overage is resolved (PRD 00 D8, PRD 05 §9.4); only the price band remains contested. The tier label is safe regardless; do not encode a price here.
    - **EU AI Act content-marking date** (Aug 2 vs Dec 2 2026, plus a May-2026 provisional reshuffle) — owned by **PRD 00/04/05**, needs legal sign-off. Not surfaced on this chat surface today; if a content-marking/watermark badge ever lands in the renderer, gate it on that decision rather than assuming a date.
    - **Whether we serve minors / companion personas** — owned by **PRD 05** (policy/competitive). Not touched by this surface; flagged so onboarding/empty-state copy (§4.7) isn't designed around an unresolved audience decision.

---

## 9. References

**Internal**
- Cross-PRDs: 02 AI Capabilities, 03 Mobile/Responsive, 04 Architecture.

**Key external sources (from research, re-verify at build)**
- Streaming-safe markdown rendering — Streamdown: https://streamdown.ai/
- Reasoning panel components — shadcn AI reasoning: https://www.shadcn.io/ai/reasoning ; AI Elements: https://elements.ai-sdk.dev/components/reasoning
- Reasoning display patterns — digestibleux: https://www.digestibleux.com/p/how-ai-models-show-their-reasoning ; Claude extended thinking: https://renezander.com/blog/claude-extended-thinking/
- Keyboard shortcuts — ai-toolbox ChatGPT shortcuts: https://www.ai-toolbox.co/chatgpt-management-and-productivity/chatgpt-keyboard-shortcuts-guide
- Editing/branching restriction (2026) — aiproductivity: https://aiproductivity.ai/news/chatgpt-restricts-message-editing-retry/
- Accessibility gaps in leaders — accessibility-test.org: https://accessibility-test.org/blog/qa-testing/automated-testing/ai-accessibility-testing-chatgpt-vs-claude-vs-gemini-oct-2025/
- Citations/source cards — unusual.ai Perplexity guide: https://www.unusual.ai/blog/perplexity-platform-guide-design-for-citation-forward-answers
- Empty-state / suggested prompts — fuselabcreative: https://fuselabcreative.com/chatbot-interface-design-guide/
- Artifacts/Canvas — Claude Artifacts: https://support.claude.com/en/articles/9487310-what-are-artifacts-and-how-do-i-use-them
